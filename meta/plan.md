# План

Один список, у порядку виконання. Кожен крок закінчується звітом і **зупинкою** – наступний
починається після «продовжуй». Причина не бюрократична: проєкт уже двічі виявляв, що прийняте
рішення суперечить вимірюванню, і межа кроку – місце, де таке знаходять дешево.

Правила роботи – `AGENTS.md`. Тут лише що і в якому порядку.

## Зроблено

**Кроки 1–4, усі 2026-09-04.**

| Крок | Що з нього лишилось у репозиторії |
|---|---|
| 1. Модель і валідатор | `tools/iqa/model.py`, `lifecycle.py`, `validate.py`, `meta/`, негативна фікстура на кожну ворота |
| 2. Вертикальний зріз | `export.py`, полагоджене дзеркало, єдиний шлях збірки, переписаний `verify_build.py`, `anki/build.py` |
| 3. CI | три workflow, ворота `pinned-actions`, `requirements/python-lock.txt` з хешами |
| 4. Міграція | 392 картки в `content/`, числа й наслідки – `meta/measurements.md` |

Поточний стан: **1261 питання обома мовами, 1248 українських карток і 1005 англійських**,
`python -m iqa build` проходить наскрізь, 108 тестів зелені.

---

## 5. Живе посилання з картки – **зроблено 2026-09-04**

Репозиторій опубліковано, GitHub Pages увімкнено, сайт задеплоєно вручну через `workflow_dispatch`.
CI відпрацював на пуші з першого разу.

Сайт: **https://bogdan-kovalchuk.github.io/interview-qa/**

Перевірено не «workflow позеленів», а самі посилання: усі **388** адрес `Reference` витягнуто
з бази `.apkg` і опитано – **388 із 388 віддають 200**. На вибірці з 12 випадкових карток
resolver веде на канонічну сторінку, і на ній є українська «Коротка відповідь». Корінь
редіректить на `/en/`, `robots.txt` і sitemap віддаються, українські сторінки мають українські
заголовки.

Автодеплой на merge **навмисно не ввімкнено**: `deploy.yml` лишається `workflow_dispatch`.
Увімкнути – один блок у тому ж файлі, і в ньому написано який.

## 6. Сайт, яким можна користуватися – **частково, у роботі**

Зроблено:

- **навігація з дерева таксономії**: згенерований `site/src/generated/sidebar.json`, два рівні
  (трек, секція) у документному порядку `meta/taxonomy.md`; індекси треків і секцій, де й перелічені
  питання; головна замість плоского списку з 401 посилання;
- **оформлення за Material for MkDocs** (`site/src/styles/`), звірене піксельно з референсом.
  Заголовки сторінки питання беруть тип за **роллю**, а не за рівнем: назва питання – як
  референсний `h3`, `Коротка відповідь`/`Розгорнуте пояснення` – як його `h4` (`meta/site.md`);
- **джерела на сторінці – один список**: markdown-виноски прибрані, бо рендерер додавав власний блок
  нижче секції і кожне процитоване джерело з'являлося двічі;
- `tools/iqa/report.py` і крок `report` у CLI; `python -m iqa build` пише
  `dist/export/progress.{json,csv}` разом з експортом;
- `python -m iqa report --todo` друкує наступні незавершені пари (питання, мова) – саме з нього
  бралася черга роботи в кроці 7;
- ненаписана секція **не рендериться на сторінці взагалі**. Спершу тут була плашка «ще не
  написано», але на 224 сторінках із 401 їх виходило чотири й більше, і вона перетворилась на шум.
  Дірки лишаються видимими як дані – `progress.{json,csv}` і майбутній `/status/`.

Pagefind збирається й перевіряється обома мовами у production build. Лишається сторінка `/status/`,
яка рендерить уже наявний `progress.json`
(`meta/site.md`). `/status/` тепер несуча: відколи ненаписані секції не рендеряться, сторінка
питання більше не показує, чого на ній бракує, і єдиним місцем, де це видно, лишається звіт.

Scale spike на 5 000 синтетичних сторінок – теж тут. На 1261 питанні (2522 сторінках) збірка ще не
показує межу, тож раніше він нічого не міряв би.

**Вихід:** сайтом можна користуватися без прямих посилань, і видно, чого бракує.

## 7. Розгорнуті пояснення – **у роботі, 136 з 392**

Робота, а не інфраструктура. Порядок дає `python -m iqa report --todo`, а не алфавіт.

Перший прохід (2026-09-05) дали агенти Sonnet партіями по секціях; бриф вимагав citation-токен лише
з наявного у файлі `sources`, побайтово однакові блоки коду між мовами і жодного дотику до
frontmatter – ревізії (`content_revision`, `reconciled_with`, `updated`) проставляє один
детермінований прохід після всього, за фактичним git-дифом.

**Стиль змінено 2026-09-06** на форму референсного сайту: означення першим реченням, короткі абзаци,
рядок-підводка перед прикладом коду, `#`-коментарі в коді, що показують результат або підступ, жирна
підводка замість підзаголовка (справжні заголовки в цій секції заборонені) і марковані списки для
переліків. Бриф з повними правилами – у робочій теці сесії, зразок стилю –
`python/decorators/order-decorators-outer-inner-written-above-same.md`.

Закриті повністю: `python/fundamentals` (12) і `python/syntax-and-control-flow` (14).

Решта 256 питань й далі мають `Detailed explanation: TODO` обома мовами. Інші секції за типом
(`Comparison`, `When to choose which`, `Symptom`, `Why it happens`, `How to avoid`, `Evaluation
guide`) не чіпали свідомо: написані питання перейшли з `stub` у `partial`, а не в `complete`.

Тут же 13 питань типу `coding` з `anki.export: false`: щойно їм допишуть `Task` і `Tests`, вони
почнуть їхати картками.

**Вихід:** визначається обсягом, який власник готовий написати. Це режим роботи, а не разовий крок.

## 7a. Імпорт embedded-карток – **завершено, 853 з 853 source records**

Перенесено 646 питань Embedded Interview Lab і 207 питань DOU. Усі вони мають українську й
англійську `Short answer`; код із заголовків винесено в `Question code`, HTML та мойбейк
нормалізовано, а `warn`-розмітку відновлено. Один дубль злитий у наявне питання без перевикористання
ID. Три нові питання embedded, додані 2026-09-08 для покриття порожніх секцій, не входять у ці 853
імпортовані записи.

Вихідні теки після перевірки покриття видалені. Одноразові конвертери, source map і worklist також
видалені 2026-09-08, бо normal build їх не використовував і відтворити імпорт без джерел вони вже не
могли. Одинадцять невирішених кандидатів на дублікати перенесено за QID до
`meta/review-backlog.md`; канонічний облік виданих ID лишається в `meta/id-registry.csv`.

## 7b. Код у питанні й двомовний embedded-трек – **зроблено 2026-09-07**

Крок 7a переніс текст; цей прибрав те, що перенесення обійшло, і зробив трек двомовним.

**Код у питанні (рішення 23).** Нова необов'язкова секція `Question code` перед `Short answer` у
`concept`, `mechanism`, `comparison`, `pitfall`: рівно один fenced block, жодного тексту довкола,
ніколи `TODO`. `Front` картки = `title` + цей блок. Пройшло весь ланцюг – `model.py` (послідовності,
`OPTIONAL_SECTIONS`, `expected_sections`, і поза `completeness`), `validate.py`, `export.py`
(`card.question_code`), `anki/build.py` (`render_front`), `meta/vocabulary.yml`, контракт §4 і два
тести. Винесено 131 блок із lab446 (з голови відповіді) і 42 з перших 407 (з `title`, де вони лежали
сирим `<pre>` і друкувалися екранованими тегами); 5 блоків, які імпорт був змушений злити в один,
розділено назад на форму джерела.

**Нормалізація перших 407.** 299 тіл із сирого HTML у Markdown контракту, 132 заголовки очищено,
407 мойбейк-`applicability` реконструйовано (15 шаблонних фраз; форма 7+7 слів зійшлася з недоторканим
англійським відповідником), 6 багаторядкових `<code>` із `<br>` стали справжніми fenced blocks.
Роблено **на місці**, а не переодиненням з джерела, бо 29 із 407 відповідей правили руками після
імпорту. Запобіжник – порівняння сигнатур слів окремо для прози й коду, до і після: розбіжностей 0.

**Дві вади лічильника речень.** Межа 2–5 рахувалася по-різному залежно від форми розмітки: позначку
межі ставило для `<code>x</code>`, але не для `` `x` ``, а зняття тегів патерном `<[^>]+>` з'їдало все
між `1<<5` і наступним `->` – одна відповідь із семи речень рахувалася як одне. Обидві виправлено,
обидві мають тест. Зачепило не лише embedded: у корпусі 9 відповідей рахуються інакше.

**Пошкодження, яке лишив ручний імпорт.** Крапки на кінцях абзаців були замінені на `;`. Жоден
конвертер цього не робить – перевірено прогоном того самого запису джерела через `htmlconv` і
`labconv`. Відновлено 37 кінців абзаців у 34 файлах і 3 зламані маркери списку (`1;` `2;` `4;`, через
які пункт злипався з попереднім в один рядок). Разом із попередніми кроками це відкрило справжню
довжину 64 відповідей – усі перероблено до 2–5 речень з'єднанням сусідніх речень або поверненням
списку з `;` на кінцях пунктів; зміст не скорочувався.

**Англійська половина.** 853 переклади `Short answer` і `description`, батчами по 20, окремий агент
на батч. Агент тільки читає батч-JSON і пише результат-JSON; файли править скрипт, який звіряє кожен
блок коду побайтово з українською половиною, набори citation-токенів, ліміт речень і заборонені
символи. Із 853 позицій ворота відхилили шість, і жодну не довелося перекладати вдруге: один
переклад коментаря всередині блока коду й три citation-токени з пробілом або без `^` відремонтовано
детерміновано з української половини (вона авторитетна саме в цих двох речах), дві відповіді з
шістьма реченнями замість п'яти поправлено вручну. Ще в 11 позиціях `en_description` прийшов із
бектиками – проєкт сам знімає розмітку з цього поля, тож знято і тут, а не повернуто агенту. Окрема
відмова цілого батча: один агент повернув файл із невалідним JSON-escape у скопійованому коді, тож
20 позицій не читалися, поки backslash не подвоєно. Глосарій спрацював тричі (`inheritance`,
`move semantics`) – виправлено в українських файлах, бо словник вимагає канонічної форми саме там.

Англійська колода виросла з 145 до 998 карток, і вперше з'явилась
`Interview QA (EN) - Embedded.apkg`. Після додавання семи двомовних карток для порожніх секцій
англійський Full Library містить 1005 карток.

## 8. Цілі співбесіди

**Не відроджувати `meta/topic-briefs/`.** Вони видалені навмисно: сума їхніх `Planned total`
дорівнювала рівно 392 – стільки ж, скільки карток у predecessor-колоді. Цілі, зроблені з них,
були б за побудовою закриті тим самим набором, і звіт покриття показав би 100%, нічого не
перевіривши. Відновити можна з git, але не варто.

Порядок зворотний: скласти інвентар цілей з вимог ролей і зовнішніх джерел, **потім** зіставити
з ним наявне 1261 питання. Цінність саме в тому, що не збіглося.

**Вихід:** список цілей, у яких немає жодного `complete` питання.

## 9. Нові треки і програми

Скелети `cpp/` і `embedded/` за `meta/taxonomy.md`, питання під закриття цілей із кроку 8. Повний
resolver `programs/*.yml`, сторінки програм, пакети на програму. Далі `cs`, `systems`,
`databases`, `engineering` до робочого обсягу.

Кількість карток – метрика, а не ціль.

---

## Поза скоупом

Версіонування сайту під кілька версій мови, інтерактивні виконувані приклади, Telegram-бот поверх
`questions.json`, аудіо чи відео, облікові записи й трекінг прогресу на сайті – прогрес живе в
Anki.

## Борг, який варто пам'ятати

- **Тег `role::` сам не зникає**, коли питання виходить зі складу програми: його немає в новому
  пакеті, а імпорт не чіпає відсутніх нотаток. Білдер має ще один реліз включати колишніх членів
  без цього тегу, беручи список із попереднього manifest. Актуально з кроку 9.
- **Діф-ворота Anki** проти базової лінії чекають на перший release manifest – він з'явиться при
  першому релізі колоди тегом `deck-v*`.
- **`dependency-audit`** відкладено свідомо (`meta/decisions.md` §21).
- **46 попереджень** про `Short answer` понад 90 слів. Лічильник приведено до контракту: code block,
  citation-токени й HTML-теги не рахуються, але видимий текст усередині `<span>` рахується.
  Скорочувати зміст лише заради м'якої межі не потрібно.
- **`bump_revisions.py` не зводить половини, написані в різних проходах.** Скрипт піднімає
  `reconciled_with` лише коли обидва мовні файли змінилися в одному проході; якщо українську
  написали вчора, а англійську сьогодні, обидві ревізії стають однаковими, але маркери лишаються
  старими, і `lang-reconciliation` лається з обох боків. Ловиться валідатором, лікується руками.

## 10. Глобальне review репозиторію – **призупинено 2026-09-08**

Review карток виявився ресурсомістким і зупинений за рішенням власника. Єдиний актуальний обсяг,
включно з уже підтвердженими дефектами та парами, яких ще не перевіряли, зафіксовано в
`meta/review-backlog.md`. Тимчасові audit-звіти не є джерелом істини.

Коли робота відновиться, виконувати backlog малими порціями, після кожної запускати механічні
ворота й зупинятися зі звітом. Після закриття всіх порцій провести центральну інтеграцію та
повторну механічну й візуальну перевірку всього репозиторію.
