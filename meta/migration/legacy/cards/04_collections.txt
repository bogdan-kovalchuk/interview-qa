#separator:tab
#html:true
#deck:Python Interview Questions
#notetype:Python Interview Basic
#tags column:3
Коли tuple є кращим вибором за list не через syntax, а через семантику незмінної структури даних?	<span class="key">Tuple варто обирати, коли дані логічно є незмінним записом (record) — його immutability сигналізує про намір і дозволяє використовувати tuple як dict key або елемент set.</span> Наприклад, координати <code>(x, y)</code>, row tuple з БД, або складений ключ словника. <span class="warn">Якщо всередині tuple є mutable об'єкт (наприклад <code>list</code>), його вміст можна змінити — сам tuple залишається тим самим об'єктом з тим самим <code>id</code>.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — tuple</a></div>	topic::04_collections type::Scenario level::Middle scope::Core card::PYI_04_001 ref::TV_PYTHON_SEQUENCES_10 source::PythonDocs source::Community
Чому <code>range</code> може представляти дуже великий діапазон без зберігання всіх integers у пам’яті?	<span class="key"><code>range</code> зберігає лише три значення — <code>start</code>, <code>stop</code>, <code>step</code> — і обчислює елементи на вимогу, тому споживає O(1) пам'яті незалежно від діапазону.</span> <code>range(10**15)</code> і <code>range(10)</code> займають однаковий обсяг пам'яті. Операції <code>in</code> (арифметична перевірка) та індексація виконуються за O(1).<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — range</a></div>	topic::04_collections type::Mechanism level::Middle scope::Core card::PYI_04_002 ref::TV_PYTHON_SEQUENCES_17 source::PythonDocs source::Community
Яка різниця між <code>list.sort()</code> і <code>sorted()</code> щодо mutation, return value та допустимих input iterables?	<span class="key"><code>list.sort()</code> сортує список на місці та повертає <code>None</code>; <code>sorted()</code> повертає новий відсортований список і приймає будь-який iterable.</span> <code>sorted()</code> працює з рядками, generators, set тощо, тоді як <code>list.sort()</code> доступний лише для <code>list</code>. Обидва методи гарантовано стабільні. <span class="warn"><code>list.sort()</code> трохи швидший, бо не створює нового списку — використовуйте його, коли початковий список більше не потрібен у старому порядку.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/howto/sorting.html">Python 3.14: Sorting HOW TO</a></div>	topic::04_collections type::Contrast level::Middle scope::Core card::PYI_04_003 ref::TV_PYTHON_SEQUENCES_11 source::PythonDocs source::Community
Чому repeated concatenation списку в циклі може бути гіршою за <code>append</code>/<code>extend</code>, навіть якщо результат однаковий?	<span class="key">Операція <code>lst = lst + [x]</code> щоразу створює новий список і копіює всі елементи, що дає O(n²) для n ітерацій.</span> Натомість <code>lst.append(x)</code> працює за амортизоване O(1), а <code>lst.extend(iterable)</code> або <code>lst += iterable</code> модифікують список на місці. <span class="warn">Саме <code>lst = lst + [x]</code> (з переприв'язкою) є повільним; <code>lst += [x]</code> — це in-place операція, еквівалентна <code>extend</code>.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — mutable sequence operations</a></div>	topic::04_collections type::Scenario level::Middle scope::Core card::PYI_04_004 ref::TV_PYTHON_SEQUENCES_11 source::PythonDocs source::Community
Як tuple, що містить mutable element, поводиться під час спроби <code>t[0] += value</code>, і чому mutation може статися до <code>TypeError</code>?	<span class="key"><code>+=</code> для mutable елемента всередині tuple спочатку виконує in-place mutation, а потім намагається присвоїти результат назад у tuple — присвоєння викликає <code>TypeError</code>, але мутація вже відбулася.</span><pre class="code-block"><code>t = ([1, 2], [3])<br>t[0] += [3]    # TypeError, але t[0] вже [1, 2, 3]</code></pre>Вираз <code>t[0] += [3]</code> фактично виконує <code>t[0] = t[0].__iadd__([3])</code>: <code>__iadd__</code> модифікує список, а потім <code>tuple.__setitem__</code> відхиляє присвоєння.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — augmented assignment</a></div>	topic::04_collections type::Code level::Senior scope::Core card::PYI_04_005 ref::TV_PYTHON_SEQUENCES_10 source::PythonDocs source::Community
Що саме гарантує insertion order словника в сучасному Python і чого ця гарантія не говорить про внутрішню hash table?	<span class="key">Починаючи з Python 3.7, <code>dict</code> зберігає елементи в порядку вставки — це гарантія мови, а не деталь реалізації.</span> Гарантія стосується порядку ітерації (<code>keys()</code>, <code>values()</code>, <code>items()</code>), але не розкриває структуру hash table. У CPython це реалізовано через компактну таблицю з масивом індексів і масивом записів; інші реалізації Python можуть досягти того ж порядку іншим шляхом.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — dict</a></div>	topic::04_collections type::Mechanism level::Middle scope::Core card::PYI_04_006 ref::TV_PYTHON_SET_AND_DICT_14 source::PythonDocs source::Community version::Py3_14 runtime::CPython
Чому keys словника мають бути hashable і як dictionary використовує hash разом з equality?	<span class="key">Ключ має бути hashable — мати <code>__hash__()</code> та <code>__eq__()</code> — тому що dict використовує hash для визначення bucket, а equality для розв'язання колізій.</span> Спочатку обчислюється <code>hash(key)</code> для пошуку позиції; якщо в bucket вже є елементи, викликається <code>__eq__()</code> для порівняння з кожним. <span class="warn">Об'єкти, що порівнюються як рівні, повинні мати однаковий hash — інакше ключ «загубиться».</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/glossary.html">Python 3.14: Glossary — hashable</a></div>	topic::04_collections type::Mechanism level::Middle scope::Core card::PYI_04_007 ref::TV_PYTHON_SET_AND_DICT_11 source::PythonDocs source::Community
Чим <code>d[key]</code>, <code>d.get(key)</code> і <code>setdefault()</code> відрізняються за поведінкою при відсутньому key та можливими side effects?	<span class="key"><code>d[key]</code> викликає <code>KeyError</code>; <code>d.get(key)</code> повертає <code>None</code> (або переданий default) без побічних ефектів; <code>d.setdefault(key, default)</code> вставляє <code>default</code> у словник, якщо ключ відсутній, і повертає його.</span> <span class="warn"><code>setdefault()</code> завжди обчислює аргумент <code>default</code>, навіть коли ключ існує — це може бути небажаним side effect, якщо створення default дороге.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — dict methods</a></div>	topic::04_collections type::Contrast level::Middle scope::Core card::PYI_04_008 ref::TV_PYTHON_SET_AND_DICT_06 source::PythonDocs source::Community
Коли <code>collections.defaultdict</code> кращий за ручну перевірку key і яку помилку може приховати неправильна default factory?	<span class="key"><code>defaultdict</code> кращий, коли потрібно групувати або акумулювати значення за ключами: він автоматично створює default-значення через <code>default_factory</code> при зверненні через <code>__getitem__</code>.</span> <span class="warn">Будь-яке звернення <code>d[key]</code> для відсутнього ключа тихо створює запис, тому випадковий lookup збільшує словник помилковими записами. Метод <code>d.get(key)</code> не викликає <code>default_factory</code> і не створює ключ.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/collections.html">Python 3.14: collections — defaultdict</a></div>	topic::04_collections type::Scenario level::Middle scope::Core card::PYI_04_009 ref::TV_PYTHON_SET_AND_DICT_09 source::PythonDocs source::Community
Чому зміна розміру словника під час ітерації його keys може завершитися помилкою, тоді як зміна value наявного key часто допустима?	<span class="key">Ітерація по словнику фіксує його розмір (кількість key-слотів); додавання або видалення ключа змінює цей розмір і викликає <code>RuntimeError: dictionary changed size during iteration</code>.</span> Зміна значення наявного ключа не змінює розмір таблиці, тому не порушує ітерацію. Безпечний патерн — ітерувати копію ключів: <code>for k in list(d):</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: stdtypes — dict</a></div>	topic::04_collections type::Trap level::Senior scope::Core card::PYI_04_010 ref::TV_PYTHON_SET_AND_DICT_08 source::PythonDocs source::Community
Як dict union operators <code>|</code> і <code>|=</code> вирішують конфлікти однакових keys та чим відрізняються за mutation?	<span class="key">Обидва оператори при дублікаті keys залишають значення з правого операнда; <code>|</code> повертає новий dict, а <code>|=</code> оновлює лівий in-place.</span> <code>d1 | d2</code> створює новий словник, <code>d1</code> не змінюється. <code>d1 |= d2</code> еквівалентний <code>d1.update(d2)</code> — модифікує <code>d1</code> на місці. Порядок keys у результаті: спочатку keys з лівого операнда в їхньому порядку, потім нові keys з правого.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Mapping Types — dict</a></div>	topic::04_collections type::Contrast level::Middle scope::Core card::PYI_04_011 ref::TV_PYTHON_SET_AND_DICT_06 source::PythonDocs source::Community
Які наслідки має custom <code>dict.__missing__()</code> і чому він не викликається однаково всіма способами доступу до key?	<span class="key"><code>__missing__(key)</code> викликається лише методом <code>__getitem__()</code>, коли key відсутній; <code>get()</code>, <code>pop()</code> та <code>setdefault()</code> його не викликають.</span> Це означає, що <code>d[missing_key]</code> запустить <code>__missing__</code>, а <code>d.get(missing_key)</code> поверне <code>None</code> (або вказаний default) без виклику <code>__missing__</code>. Саме на цьому механізмі побудований <code>collections.defaultdict</code>: його <code>__missing__</code> викликає <code>default_factory</code> і записує результат у словник.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Mapping Types — dict</a></div>	topic::04_collections type::Mechanism level::Senior scope::Core card::PYI_04_012 ref::TV_PYTHON_SET_AND_DICT_09 source::PythonDocs source::Community
Коли <code>set</code> є кращим за list для membership checks і який порядок елементів не слід використовувати як контракт?	<span class="key"><code>set</code> дає average-case O(1) для <code>in</code> завдяки hash-таблиці, тоді як list — O(n); тому для частих перевірок membership set значно швидший.</span> <span class="warn">Порядок ітерації set є implementation-dependent і не гарантує ні сортування, ні порядку вставки — його не можна використовувати як контракт.</span> На практиці: якщо потрібен і швидкий lookup, і стабільний порядок, використовують dict (з Python 3.7+ зберігає порядок вставки) або list з попереднім індексуванням.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Set Types — set, frozenset</a></div>	topic::04_collections type::Scenario level::Middle scope::Core card::PYI_04_013 ref::TV_PYTHON_SET_AND_DICT_02 source::PythonDocs source::Community runtime::CPython
Чим <code>set</code> відрізняється від <code>frozenset</code> щодо mutability та можливості бути елементом іншого set?	<span class="key"><code>set</code> — mutable і не hashable, тому не може бути елементом іншого set; <code>frozenset</code> — immutable й hashable, тому може.</span> Спроба додати <code>set</code> у інший set викличе <code>TypeError: unhashable type: 'set'</code>. <code>frozenset</code> підтримує ті самі set-операції (union, intersection, subset), але не має методів <code>add()</code>, <code>remove()</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Set Types — set, frozenset</a></div>	topic::04_collections type::Contrast level::Middle scope::Core card::PYI_04_014 ref::TV_PYTHON_SET_AND_DICT_03 source::PythonDocs source::Community
Як set operations дозволяють виразити перевірку subset/superset без ручних nested loops?	<span class="key">Оператори <code>&lt;=</code> (<code>issubset</code>) і <code>&gt;=</code> (<code>issuperset</code>) виконують перевірку включення всіх елементів за один виклик, без явних циклів.</span> <code>a &lt;= b</code> повертає <code>True</code>, якщо кожен елемент <code>a</code> міститься в <code>b</code>. Оператор <code>&lt;</code> — strict (proper) subset: <code>a &lt; a</code> дає <code>False</code>, тоді як <code>a &lt;= a</code> — <code>True</code>. Це працює за O(len(a)) у середньому, бо кожен <code>in</code> для set — O(1).<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Set Types — set, frozenset</a></div>	topic::04_collections type::Scenario level::Middle scope::Core card::PYI_04_015 ref::TV_PYTHON_SET_AND_DICT_03 source::PythonDocs source::Community
Чому некоректний <code>__eq__</code> у custom objects може призвести до несподіваних результатів membership у set?	<span class="key">Set шукає елемент спочатку за <code>hash()</code> (bucket), потім порівнює <code>__eq__</code>; якщо <code>__eq__</code> і <code>__hash__</code> неузгоджені, membership дає хибні результати.</span> Контракт: <code>a == b</code> ⇒ <code>hash(a) == hash(b)</code>. Якщо <code>__eq__</code> повертає <code>True</code>, але <code>hash()</code> різний, set не знайде об'єкт у правильному bucket — <code>b in {a}</code> поверне <code>False</code>, хоча <code>a == b</code>. <span class="warn">Завжди визначайте <code>__hash__</code> і <code>__eq__</code> разом, використовуючи одні й ті самі поля.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Hashing of numeric types</a></div>	topic::04_collections type::Trap level::Senior scope::Core card::PYI_04_016 ref::TV_PYTHON_SET_AND_DICT_15 source::PythonDocs source::Community
Чи є <code>dict.keys()</code>, <code>dict.values()</code> і <code>dict.items()</code> snapshots, і як їхня live-view природа впливає на подальші зміни словника?	<span class="key">Ні, це live views — вони миттєво відображають будь-які зміни словника, а не знімок на момент виклику.</span> Додавання або видалення key у словнику одразу видно через збережений об'єкт view. <code>dict.keys()</code> і <code>dict.items()</code> підтримують set-операції (union, intersection). <span class="warn">Зміна словника під час ітерації його view викликає <code>RuntimeError</code>.</span> Для snapshot потрібно явно створити копію: <code>list(d.keys())</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Dictionary view objects</a></div>	topic::04_collections type::Mechanism level::Middle scope::Core card::PYI_04_017 ref::TV_PYTHON_SET_AND_DICT_07 source::PythonDocs source::Community
Що повинен повертати <code>__iter__</code> custom collection і чому повернення самого reusable container зазвичай некоректне?	<span class="key"><code>__iter__</code> має повертати новий iterator (об'єкт з <code>__next__</code>) при кожному виклику, щоб незалежні ітерації не конфліктували.</span> Якщо container повертає <code>self</code>, він стає iterator із одним спільним станом: після першого <code>list(obj)</code> iterator вичерпується, і друга ітерація дасть порожній результат. Правильна реалізація: <code>def __iter__(self): return iter(self._data)</code> — створює окремий iterator для кожного виклику.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Iterator Types</a></div>	topic::04_collections type::Mechanism level::Senior scope::Core card::PYI_04_018 ref::OFFICIAL source::PythonDocs
Як sequence comparison працює lexicographically і що станеться при першій парі елементів, які не підтримують ordering?	<span class="key">Послідовності порівнюються лексикографічно: елемент за елементом, поки не знайдеться перша нерівна пара — вона й визначає результат.</span> Якщо всі спільні елементи рівні, коротша послідовність вважається меншою: <code>[1, 2] &lt; [1, 2, 3]</code> → <code>True</code>. Якщо пара елементів не підтримує ordering (наприклад, <code>int</code> і <code>str</code>), виникає <code>TypeError</code>. Порівняння різних типів послідовностей (<code>list</code> vs <code>tuple</code>) також викликає <code>TypeError</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/expressions.html">Python 3.14: Value comparisons</a></div>	topic::04_collections type::Mechanism level::Middle scope::Core card::PYI_04_019 ref::TV_PYTHON_SEQUENCES_19 source::PythonDocs source::Community
Що означає stability сортування Python і як використати її для multi-key sorting без custom comparator?	<span class="key">Stability означає, що елементи з однаковим key зберігають початковий відносний порядок; це дозволяє сортувати за кількома keys послідовно, від менш значущого до більш значущого.</span> Наприклад, щоб сортувати за спаданням <code>grade</code> і зростанням <code>age</code>: спочатку <code>sorted(data, key=age)</code>, потім <code>sorted(result, key=grade, reverse=True)</code>. Stability гарантує, що порядок за <code>age</code> збережеться для однакових <code>grade</code>. Альтернатива для одного напрямку — tuple key: <code>key=itemgetter(grade, age)</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/howto/sorting.html">Python 3.14: Sorting HOW TO</a></div>	topic::04_collections type::Mechanism level::Middle scope::Core card::PYI_04_020 ref::TV_PYTHON_SEQUENCES_11 source::PythonDocs source::Community
Коли key function у <code>sorted()</code> краща за реалізацію rich comparison methods у domain class?	<span class="key">Key function краща, коли порядок сортування залежить від контексту або зовнішніх даних, а не від природного порядку самого класу.</span> Rich comparison methods (наприклад, <code>__lt__</code>) визначають єдиний канонічний порядок класу; key function дозволяє сортувати за будь-яким похідним ключем без зміни класу. Наприклад, <code>sorted(users, key=lambda u: scores[u.id])</code> сортує за зовнішнім словником, що неможливо виразити через <code>__lt__</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/howto/sorting.html">Python 3.14: Sorting HOW TO</a></div>	topic::04_collections type::Scenario level::Senior scope::Core card::PYI_04_021 ref::OFFICIAL source::PythonDocs
Чому membership у list зазвичай має linear cost, а в set або dict очікується average constant-time lookup?	<span class="key">Для <code>list</code> оператор <code>in</code> виконує лінійний пошук: порівнює <code>x</code> з кожним елементом через <code>==</code> — O(n).</span> <code>set</code> і <code>dict</code> у CPython використовують hash-таблицю: обчислюється <code>hash(key)</code>, за ним знаходиться бакет, і лише там викликається <code>==</code> — average O(1). <span class="warn">У найгіршому випадку (багато collision-ів) lookup у hash-таблиці деградує до O(n).</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Built-in Types</a></div>	topic::04_collections type::Contrast level::Middle scope::Core card::PYI_04_022 ref::TV_PYTHON_SET_AND_DICT_18 source::PythonDocs source::Community runtime::CPython
Коли <code>collections.deque</code> кращий за list для роботи з обома кінцями послідовності?	<span class="key"><code>deque</code> гарантує O(1) append і pop з обох кінців, тоді як <code>list</code> вимагає O(n) для <code>insert(0, v)</code> та <code>pop(0)</code> через зсув елементів у масиві.</span> <code>deque</code> кращий для черг, ковзних вікон та алгоритмів, де потрібна ефективна робота з обома кінцями. <span class="warn"><code>deque</code> має O(n) доступ до середини за індексом, тому для random access list залишається кращим.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/collections.html">Python 3.14: collections</a></div>	topic::04_collections type::Scenario level::Middle scope::Core card::PYI_04_023 ref::OFFICIAL source::PythonDocs
Чому hash-flooding або велика кількість collisions може зруйнувати expected O(1) lookup і чому це не змінює логічний contract словника?	<span class="key">Hash-flooding — це ситуація, коли багато ключів потрапляють в один бакет hash-таблиці, і lookup деградує з average O(1) до O(n) через довгі probe-послідовності.</span> У CPython dict і set використовують open addressing, тому collision-и змушують інтерпретатор шукати вільний слот зондуванням. Логічний contract словника при цьому не порушується: <code>__eq__</code> перевіряється для кожного кандидата в бакеті, тому правильний ключ знаходиться — змінюється лише швидкодія, а не коректність.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/stdtypes.html">Python 3.14: Built-in Types</a></div>	topic::04_collections type::Mechanism level::Senior scope::Core card::PYI_04_024 ref::TV_PYTHON_SET_AND_DICT_16 source::PythonDocs source::Community runtime::CPython
