#separator:tab
#html:true
#deck:Python Interview Questions
#notetype:Python Interview Basic
#tags column:3
Коли <code>collections.ChainMap</code> кращий за eager merge dictionaries для layered configuration з precedence між command line, environment і defaults?	<span class="key"><code>ChainMap</code> шукає ключі послідовно в кожному словнику від першого до останнього, не створюючи нового merged dict, тому зміни в нижніх шарах автоматично відображаються без повторного merge.</span> Lookups йдуть у порядку precedence (наприклад, CLI → env → defaults), а writes і deletions завжди зачіпають лише перший mapping. Це зручно для nested scopes або layered config, де шари можуть оновлюватися незалежно.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/collections.html#collections.ChainMap">Python 3.14: collections.ChainMap</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_001 ref::TV_PYTHON_STANDARD_LIBRARY_03 source::PythonDocs source::Community
Як <code>Counter</code> спрощує frequency analysis і як він поводиться з missing keys?	<span class="key"><code>Counter</code> повертає <code>0</code> для відсутніх ключів замість <code>KeyError</code>, що дозволяє рахувати елементи без перевірки наявності ключа.</span> Ініціалізується з iterable або mapping, має методи <code>most_common(n)</code> для топ-N елементів та <code>subtract()</code> для віднімання лічильників. Підтримує арифметичні операції (<code>+</code>, <code>-</code>, <code>&amp;</code>, <code>|</code>) між Counter-об'єктами.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/collections.html#collections.Counter">Python 3.14: collections.Counter</a></div>	topic::17_standard_library type::Mechanism level::Middle scope::Core card::PYI_17_002 ref::TV_PYTHON_STANDARD_LIBRARY_03 source::PythonDocs source::Community
Коли factory у <code>defaultdict</code> може створити небажану mutation при простому читанні key?	<span class="key">Будь-яке звернення через <code>d[key]</code> для відсутнього ключа викликає <code>default_factory</code> і додає новий запис у словник, навіть якщо ви лише «читали» значення.</span> Наприклад, <code>if d[key]:</code> для ще не існуючого ключа створить entry з дефолтним значенням. Метод <code>get()</code> не викликає factory і не мутує словник — використовуйте його для безпечного читання.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/collections.html#collections.defaultdict">Python 3.14: collections.defaultdict</a></div>	topic::17_standard_library type::Trap level::Senior scope::Core card::PYI_17_003 ref::TV_PYTHON_STANDARD_LIBRARY_03 source::PythonDocs source::Community
Коли <code>itertools.chain()</code> доречніший за конкатенацію матеріалізованих collections?	<span class="key"><code>chain()</code> повертає лінивий ітератор, який обходить вхідні iterable послідовно без створення проміжного списку, що економить пам'ять.</span> Доречний, коли потрібно об'єднати generators або великі послідовності, де конкатенація <code>a + b</code> вимагала б матеріалізації обох колекцій. Для <code>chain.from_iterable()</code> достатньо одного iterable з iterable-елементами.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/itertools.html#itertools.chain">Python 3.14: itertools.chain</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_004 ref::TV_PYTHON_STANDARD_LIBRARY_02 source::PythonDocs source::Community
Чим <code>islice()</code> корисний для лінивого iterable, який не підтримує indexing?	<span class="key"><code>islice()</code> дозволяє брати перші N елементів або діапазон з будь-якого iterable без перетворення його на список.</span> Працює як slice-синтаксис для списків, але для ітераторів: <code>islice(gen, 5)</code> дає перші 5, <code>islice(gen, 2, 10, 2)</code> — діапазон з кроком. Не підтримує від'ємні індекси, бо не може обійти iterable з кінця.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/itertools.html#itertools.islice">Python 3.14: itertools.islice</a></div>	topic::17_standard_library type::Mechanism level::Middle scope::Core card::PYI_17_005 ref::TV_PYTHON_STANDARD_LIBRARY_02 source::PythonDocs source::Community
Чому <code>itertools.groupby()</code> групує лише consecutive equal keys і коли input потрібно попередньо відсортувати?	<span class="key"><code>groupby()</code> створює нову групу щоразу, коли змінюється значення ключа — як Unix <code>uniq</code>, а не SQL <code>GROUP BY</code>.</span> Щоб усі елементи з однаковим ключем потрапили в одну групу, input потрібно відсортувати за тим самим key function. Група є ітератором, що розділяє underlying iterable з <code>groupby</code>; якщо дані потрібні пізніше, їх треба одразу зберегти в <code>list</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/itertools.html#itertools.groupby">Python 3.14: itertools.groupby</a></div>	topic::17_standard_library type::Trap level::Senior scope::Core card::PYI_17_006 ref::TV_PYTHON_STANDARD_LIBRARY_02 source::PythonDocs source::Community
Чим <code>cached_property</code> відрізняється від <code>property</code> за caching, attribute writes та invalidation?	<span class="key"><code>cached_property</code> обчислює значення один раз і зберігає його в <code>__dict__</code> інстансу, тоді як <code>property</code> викликає getter при кожному зверненні.</span> Запис атрибута перезаписує кеш (на відміну від <code>property</code>, який викличе setter). Інвалідація — через <code>del instance.attr</code>, після чого наступне звернення знову обчислить значення. Потребує mutable <code>__dict__</code>, тому не працює з <code>__slots__</code> без <code>__dict__</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/functools.html#functools.cached_property">Python 3.14: functools.cached_property</a></div>	topic::17_standard_library type::Contrast level::Middle scope::Core card::PYI_17_008 ref::TV_PYTHON_STANDARD_LIBRARY_14 source::PythonDocs source::Community
Який boilerplate усуває <code>functools.total_ordering</code> і які performance та traceback trade-offs варто врахувати?	<span class="key"><code>@total_ordering</code> автоматично генерує відсутні методи порівняння (<code>__lt__</code>, <code>__le__</code>, <code>__gt__</code>, <code>__ge__</code>) на основі одного визначеного плюс <code>__eq__</code>.</span> Trade-off: декоратор сповільнює виклик порівняння через додатковий рівень інкапсуляції та ускладнює traceback. Для hot path, де порівняння викликаються мільйони разів, краще реалізувати всі методи явно.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/functools.html#functools.total_ordering">Python 3.14: functools.total_ordering</a></div>	topic::17_standard_library type::Scenario level::Senior scope::Core card::PYI_17_009 ref::TV_PYTHON_STANDARD_LIBRARY_14 source::PythonDocs source::Community
Чому <code>pathlib.Path</code> краще передає path intent, ніж звичайний string?	<span class="key"><code>Path</code> — це об'єктна абстракція шляху, яка розрізняє операції з шляхом і роботу з файловою системою, на відміну від string, де шлях — просто текст.</span> Автоматично обирає <code>PosixPath</code> або <code>WindowsPath</code> залежно від ОС, підтримує оператор <code>/</code> для join, має методи <code>exists()</code>, <code>is_file()</code>, <code>suffix</code>, <code>parent</code>, <code>with_suffix()</code>. Шляхи immutable й hashable, тому придатні як ключі dict.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/pathlib.html">Python 3.14: pathlib</a></div>	topic::17_standard_library type::Contrast level::Middle scope::Core card::PYI_17_010 ref::TV_PYTHON_STANDARD_LIBRARY_09 source::PythonDocs source::Community
Коли для filesystem operation варто лишитися на <code>os</code>, а коли обрати <code>pathlib</code>?	<span class="key"><code>pathlib</code> кращий для маніпуляцій зі шляхами, побудови нових шляхів і читабельності; <code>os</code> доречний для низькорівневих операцій з файловими дескрипторами або коли API вимагає string.</span> Більшість сучасних функцій Python приймають <code>os.PathLike</code>, тому <code>Path</code> інтегрується без перетворення. Для <code>os.environ</code>, <code>os.fspath()</code> або legacy API, що очікують str, доводиться викликати <code>str(path)</code> або <code>os.fspath(path)</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/pathlib.html">Python 3.14: pathlib</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_011 ref::TV_PYTHON_STANDARD_LIBRARY_07 source::PythonDocs source::Community
Чим naive <code>datetime</code> відрізняється від aware <code>datetime</code> і чому їх небезпечно змішувати?	<span class="key">Naive <code>datetime</code> не має інформації про часову зону (<code>tzinfo is None</code> або <code>utcoffset()</code> повертає <code>None</code>), тоді як aware має конкретний <code>tzinfo</code> з визначеним зсувом.</span> Порівняння порядку (<code>&lt;</code>, <code>&gt;</code>) та віднімання між naive і aware об'єктами піднімає <code>TypeError</code>. Equality (<code>==</code>, <code>!=</code>) помилки не викликає, але naive і aware завжди вважаються нерівними — це тиха логічна помилка.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/datetime.html">Python 3.14: datetime</a></div>	topic::17_standard_library type::Contrast level::Middle scope::Core card::PYI_17_012 ref::TV_PYTHON_STANDARD_LIBRARY_05 source::PythonDocs source::Community
Чому <code>zoneinfo.ZoneInfo</code> краще за фіксований UTC offset для civil time з daylight-saving transitions?	<span class="key"><code>ZoneInfo</code> використовує базу даних IANA з повною історією переходів DST, тому автоматично коректує зсув під час арифметики datetime.</span> Фіксований <code>timezone(timedelta(hours=...))</code> ніколи не змінюється: якщо додати <code>timedelta(days=1)</code> через межу DST, «wall clock» покаже неправильний місцевий час. <code>ZoneInfo</code> також підтримує <code>fold</code> для неоднозначних моментів під час «відкату» годинника.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/zoneinfo.html">Python 3.14: zoneinfo</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_013 ref::TV_PYTHON_STANDARD_LIBRARY_05 source::PythonDocs source::Community
Коли <code>heapq</code> доречніший за повне сортування для top-k або priority queue?	<span class="key"><code>heapq</code> дає O(log n) для <code>heappush</code>/<code>heappop</code>, тоді як повне сортування — O(n log n); для top-k при малому k функції <code>nlargest</code>/<code>nsmallest</code> ефективніші за <code>sorted()[:k]</code>.</span> Heap доречний, коли дані надходять потоком і потрібно постійно вставляти елементи та витягувати мінімум/максимум без пересортування всього масиву. При <code>n == 1</code> простіше використати <code>min()</code>/<code>max()</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/heapq.html">Python 3.14: heapq</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_014 ref::TV_PYTHON_STANDARD_LIBRARY_01 source::PythonDocs source::Community
Коли <code>bisect</code> корисний для sorted list і чому binary search не робить insertion дешевою?	<span class="key"><code>bisect</code> знаходить позицію вставки за O(log n), але сам <code>list.insert()</code> коштує O(n) через зсув елементів у масиві.</span> Модуль корисний, коли порівняння елементів дорогі або потрібно підтримувати відсортований список без повного пересортування після кожної вставки. Для частих вставок у великий список краще обрати <code>SortedList</code> з бібліотеки <code>sortedcontainers</code> або іншу структуру з O(log n) insertion.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/bisect.html">Python 3.14: bisect</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_015 ref::TV_PYTHON_STANDARD_LIBRARY_01 source::PythonDocs source::Community
Чому library code має логувати через named logger, а не налаштовувати root logger при import?	<span class="key"><code>logging.getLogger(__name__)</code> створює logger у ієрархії пакетів, а виклик <code>basicConfig()</code> або додавання handler до root logger при import змінює логування всієї програми.</span> Бібліотека має лише генерувати log events; додаток вирішує, куди і з яким рівнем їх виводити. Ієрархія logger-ів (<code>mylib.submodule</code> → <code>mylib</code> → root) дозволяє застосунку точково налаштовувати рівень і handler для кожної гілки.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/logging.html">Python 3.14: logging</a></div>	topic::17_standard_library type::Scenario level::Middle scope::Core card::PYI_17_016 ref::TV_PYTHON_STANDARD_LIBRARY_11 source::PythonDocs source::Community
Коли обрати <code>dataclass</code> для value-like record, а коли <code>Enum</code> для закритого набору symbolic values?	<span class="key"><code>dataclass</code> автоматично генерує <code>__init__</code>, <code>__repr__</code>, <code>__eq__</code> для класів із даними; <code>Enum</code> зв'язує скінченну кількість символічних імен з унікальними константами.</span> <code>dataclass</code> підходить, коли потрібно згрупувати кілька полів у запис (наприклад, <code>Point(x, y)</code>). <code>Enum</code> — коли набір значень відомий наперед і вичерпний (стани, статуси, опції); члени Enum ітерабельні та порівнювані за ідентичністю.<div class="source">Джерела: <a href="https://docs.python.org/3.14/library/dataclasses.html">Python 3.14: dataclasses</a>; <a href="https://docs.python.org/3.14/library/enum.html">Python 3.14: enum</a></div>	topic::17_standard_library type::Contrast level::Middle scope::Core card::PYI_17_017 ref::TV_PYTHON_STANDARD_LIBRARY_01 source::PythonDocs source::Community
Коли runtime introspection через <code>inspect.signature()</code> виправдана, а коли explicit protocol зменшує coupling до implementation details?	<span class="key"><code>inspect.signature()</code> виправдана у generic-фреймворках (DI-контейнери, декоратори, wrapper-и), які мають адаптуватися до довільних callable без заздалегідь відомого інтерфейсу.</span> Якщо ж компоненту потрібна лише певна поведінка (наприклад, метод <code>.read()</code>), explicit <code>Protocol</code> або ABC дає статичну перевірку типів і не прив'язує код до конкретної сигнатури. Introspection також залежить від CPython-деталей (наприклад, <code>__wrapped__</code>) і може не працювати з C-extension.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/inspect.html">Python 3.14: inspect</a></div>	topic::17_standard_library type::Scenario level::Senior scope::Core card::PYI_17_018 ref::TV_PYTHON_STANDARD_LIBRARY_15 source::PythonDocs source::Community runtime::CPython
Чому pattern з nested або overlapping quantifiers може спричинити catastrophic backtracking на crafted input і як зменшити ReDoS risk?	<span class="key">NFA-рушій <code>re</code> перебирає експоненційну кількість шляхів backtracking, коли вкладені квантифікатори або альтернативи перекриваються на спеціально сконструйованому input.</span> Наприклад, <code>(a+)+$</code> на рядку <code>"aaa...!"</code> подвоює кількість станів із кожним символом. Починаючи з Python 3.11, можна застосовувати possessive квантифікатори (<code>*+</code>, <code>++</code>, <code>{m,n}+</code>) та atomic groups (<code>(?&gt;...)</code>), які забороняють backtracking. Загальне правило: уникати «evil regex» — груп із квантифікаторами, всередині яких є вкладені квантифікатори або альтернативи, що перекриваються.<div class="source">Джерела: <a href="https://docs.python.org/3.14/library/re.html">Python 3.14: re</a>; <a href="https://owasp.org/www-community/attacks/Regular_expression_Denial_of_Service_-_ReDoS">OWASP: ReDoS</a></div>	topic::17_standard_library type::Trap level::Senior scope::Core card::PYI_17_019 ref::OFFICIAL source::PythonDocs source::OfficialDocs
