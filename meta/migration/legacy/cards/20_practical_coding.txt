#separator:tab
#html:true
#deck:Python Interview Questions
#notetype:Python Interview Basic
#tags column:3
Що надрукує <code>events = []; enabled = False; enabled and events.append("run"); print(events)</code> і чому side effect не відбувся?	<span class="key">Надрукує <code>[]</code> — метод <code>append</code> не викликається.</span> Оператор <code>and</code> у Python використовує short-circuit evaluation: якщо лівий операнд хибний (<code>False</code>), правий операнд не обчислюється взагалі. Оскільки <code>enabled</code> дорівнює <code>False</code>, вираз <code>events.append("run")</code> ніколи не виконується, і список залишається порожнім.<div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/expressions.html#boolean-operations">Python 3.14: Boolean operations</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_001 ref::TV_PYTHON_CODING_01 source::PythonDocs source::Community
Що надрукує <code>a = [[1]]; b = a.copy(); b[0].append(2); print(a)</code> і як shallow copy створює цей aliasing bug?	<span class="key">Надрукує <code>[[1, 2]]</code> — <code>a</code> теж бачить зміну.</span> Метод <code>list.copy()</code> створює shallow copy: новий зовнішній список, але вкладені об'єкти копіюються лише за посиланням. Тому <code>b[0]</code> і <code>a[0]</code> — це той самий об'єкт <code>list</code>, і <code>append(2)</code> через <code>b</code> мутує його спільно. <span class="warn">Для глибокого копіювання потрібний <code>copy.deepcopy()</code>.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/copy.html">Python 3.14: copy</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_002 ref::OFFICIAL source::PythonDocs
У Python 3.14 що надрукує <code>match 1:<br>&nbsp;&nbsp;case x: pass<br>print(x)</code> і як capture pattern впливає на scope імені?	<span class="key">Надрукує <code>1</code> — bare name у <code>case</code> є capture pattern і прив'язує subject до імені <code>x</code>.</span> На відміну від звичайних блоків, <code>match/case</code> не створює окремого scope: змінна <code>x</code>, оголошена в <code>case x:</code>, залишається доступною після всього <code>match</code>. <span class="warn">Щоб збігтися з літералом, потрібен dotted name (наприклад, <code>case Color.RED</code>); bare name завжди є capture.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/compound_stmts.html#capture-patterns">Python 3.14: Capture patterns</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_003 ref::OFFICIAL source::PythonDocs version::Py3_14
Клас <code>Key</code> визначає <code>__eq__</code> за <code>value</code>, але не визначає <code>__hash__</code>: що станеться при <code>{Key(1): "x"}</code> і як виправити contract для immutable key?	<span class="key">Піднімається <code>TypeError: unhashable type: 'Key'</code>.</span> Коли клас визначає <code>__eq__</code>, CPython автоматично встановлює <code>__hash__ = None</code>, роблячи об'єкти unhashable. Для immutable key потрібно явно визначити <code>__hash__</code>, узгоджений з <code>__eq__</code>: хешувати ті самі поля, що порівнюються в <code>__eq__</code>. Наприклад, <code>def __hash__(self): return hash(self.value)</code>.<div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/datamodel.html#object.__hash__">Python 3.14: object.__hash__</a></div>	topic::20_practical_coding type::Code level::Senior scope::Core card::PYI_20_004 ref::TV_PYTHON_CODING_17 source::PythonDocs source::Community runtime::CPython
Реалізуйте FizzBuzz для integers 1..N з ordered rules <code>[(3, "Fizz"), (5, "Buzz")]</code>, щоб нове правило не вимагало нової branch.	<span class="key">Data-driven підхід: для кожного числа конкатенувати label усіх правил, де <code>i % divisor == 0</code>, і fallback на <code>str(i)</code>, якщо рядок порожній.</span><pre class="code-block"><code>def fizzbuzz(n, rules=None):<br>    if rules is None:<br>        rules = [(3, "Fizz"), (5, "Buzz")]<br>    result = []<br>    for i in range(1, n + 1):<br>        out = "".join(label for d, label in rules if i % d == 0)<br>        result.append(out or str(i))<br>    return result</code></pre>Додавання нового правила — просто елемент у списку; жодної <code>if/elif</code> гілки змінювати не треба.<div class="source">Джерело: <a href="https://docs.python.org/3.14/tutorial/controlflow.html">Python 3.14: Control flow</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_005 ref::TV_PYTHON_CODING_02 source::PythonDocs source::Community
Реалізуйте palindrome check: застосуйте Unicode NFKC, <code>casefold()</code>, відкиньте non-alphanumeric characters і не створюйте reversed copy normalized string.	<span class="key">Нормалізувати через <code>unicodedata.normalize("NFKC", s).casefold()</code>, потім two-pointer scan з обох кінців, пропускаючи non-alphanumeric символи.</span><pre class="code-block"><code>import unicodedata<br><br>def is_palindrome(s: str) -&gt; bool:<br>    normalized = unicodedata.normalize("NFKC", s).casefold()<br>    lo, hi = 0, len(normalized) - 1<br>    while lo &lt; hi:<br>        while lo &lt; hi and not normalized[lo].isalnum():<br>            lo += 1<br>        while lo &lt; hi and not normalized[hi].isalnum():<br>            hi -= 1<br>        if normalized[lo] != normalized[hi]:<br>            return False<br>        lo += 1<br>        hi -= 1<br>    return True</code></pre>NFKC згортає сумісні символи (наприклад, повноширинні літери), <code>casefold()</code> дає case-insensitive порівняння, а two-pointer уникає створення фільтрованої копії рядка.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/unicodedata.html">Python 3.14: unicodedata</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_006 ref::TV_PYTHON_CODING_05 source::PythonDocs source::Community
Реалізуйте суму integers у довільно вкладених lists: <code>bool</code> відхиляти, інші types мають викликати <code>TypeError</code>, а cycle у lists – <code>ValueError</code>.	<span class="key">Рекурсивний обхід з перевіркою <code>isinstance(obj, bool)</code> перед <code>isinstance(obj, int)</code> (бо <code>bool</code> — підклас <code>int</code>) та множиною <code>id()</code> для виявлення циклів.</span><pre class="code-block"><code>def nested_sum(data):<br>    def _sum(obj, seen):<br>        if isinstance(obj, bool):<br>            raise TypeError("bool is not allowed")<br>        if isinstance(obj, int):<br>            return obj<br>        if isinstance(obj, list):<br>            oid = id(obj)<br>            if oid in seen:<br>                raise ValueError("cycle detected")<br>            seen = seen | {oid}<br>            return sum(_sum(item, seen) for item in obj)<br>        raise TypeError(<br>            f"unsupported type: {type(obj).__name__}"<br>        )<br>    return _sum(data, set())</code></pre><span class="warn">Порядок <code>isinstance</code> критичний: <code>bool</code> перевіряється першим, інакше <code>True</code>/<code>False</code> пройде як <code>int</code>.</span><div class="source">Джерело: <a href="https://docs.python.org/3.14/library/functions.html#isinstance">Python 3.14: isinstance</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_007 ref::TV_PYTHON_CODING_12 source::PythonDocs source::Community
Спроєктуйте copy operation для tree з shared subtrees та possible cycles, зберігши graph topology без aliasing з original.	<span class="key">Використати memo-dict, ключований за <code>id()</code> оригінального вузла: перед рекурсивним копіюванням перевіряти, чи вузол вже скопійований, і повертати існуючу копію.</span><pre class="code-block"><code>def deep_copy_graph(root):<br>    memo = {}<br>    def _copy(node):<br>        nid = id(node)<br>        if nid in memo:<br>            return memo[nid]<br>        if isinstance(node, list):<br>            new_list = []<br>            memo[nid] = new_list<br>            new_list.extend(_copy(item) for item in node)<br>            return new_list<br>        if isinstance(node, dict):<br>            new_dict = {}<br>            memo[nid] = new_dict<br>            for k, v in node.items():<br>                new_dict[_copy(k)] = _copy(v)<br>            return new_dict<br>        return node<br>    return _copy(root)</code></pre>Memo гарантує: shared subtree копіюється один раз (ідентичність зберігається в копії), а cycle не викликає нескінченну рекурсію, бо <code>memo[nid]</code> заповнюється до рекурсивного спуску.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/copy.html">Python 3.14: copy</a></div>	topic::20_practical_coding type::Scenario level::Senior scope::Core card::PYI_20_008 ref::TV_PYTHON_CODING_13 source::PythonDocs source::Community
Реалізуйте retry decorator з tuple transient exception types і <code>max_attempts</code>: він має зберегти metadata та re-raise останній exception з його traceback.	<span class="key">Параметризований decorator: зовнішня функція приймає <code>max_attempts</code> і <code>exceptions</code>, внутрішня — <code>functools.wraps</code> для збереження metadata, цикл з <code>try/except</code> і <code>raise</code> на останній спробі.</span><pre class="code-block"><code>import functools<br><br>def retry(max_attempts, exceptions):<br>    def decorator(func):<br>        @functools.wraps(func)<br>        def wrapper(*args, **kwargs):<br>            for attempt in range(1, max_attempts + 1):<br>                try:<br>                    return func(*args, **kwargs)<br>                except exceptions:<br>                    if attempt == max_attempts:<br>                        raise<br>        return wrapper<br>    return decorator</code></pre><code>except exceptions</code> працює з tuple типів; bare <code>raise</code> на останній спробі зберігає оригінальний traceback. <code>functools.wraps</code> копіює <code>__name__</code>, <code>__doc__</code> та інші атрибути.<div class="source">Джерело: <a href="https://docs.python.org/3.14/library/functools.html#functools.wraps">Python 3.14: functools.wraps</a></div>	topic::20_practical_coding type::Code level::Senior scope::Core card::PYI_20_009 ref::TV_PYTHON_CODING_10 source::PythonDocs source::Community
У descriptor з <code>__set__(self, obj, value): self._value = value</code> два instances бачать останнє записане value: чому так сталося і де зберігати per-instance state?	<span class="key"><code>self</code> у descriptor — це сам descriptor-об'єкт (один на клас), тому <code>self._value</code> є спільним для всіх instances класу-власника.</span> Descriptor створюється один раз як class attribute, і <code>__set__</code> викликається з різними <code>obj</code>, але <code>self._value</code> перезаписується щоразу. <span class="warn">Per-instance state треба зберігати на <code>obj</code>:</span> наприклад, через <code>obj.__dict__[self.name] = value</code> або через <code>WeakKeyDictionary</code>, ключований за <code>obj</code>, щоб уникнути витоків пам'яті.<div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/datamodel.html#implementing-descriptors">Python 3.14: Implementing descriptors</a></div>	topic::20_practical_coding type::Trap level::Middle scope::Core card::PYI_20_010 ref::TV_PYTHON_CODING_18 source::PythonDocs source::Community runtime::CPython
Реалізуйте top-3 words для ASCII text: token матчить <code>[A-Za-z]+</code>, comparison є case-insensitive, а ties сортуються lexicographically.	<span class="key">Використовуємо <code>re.findall(r'[A-Za-z]+', text)</code> для токенізації, <code>Counter</code> з <code>.lower()</code> для підрахунку, та <code>sorted</code> з ключем <code>(-count, word)</code> для lexicographic tie-breaking.</span> <code>Counter.most_common()</code> не гарантує порядку для ties, тому явно сортуємо за спаданням частоти та зростанням слова.<pre class="code-block"><code>import re<br>from collections import Counter<br><br>def top3_words(text):<br>    words = re.findall(r'[A-Za-z]+', text)<br>    counts = Counter(w.lower() for w in words)<br>    sorted_items = sorted(counts.items(), key=lambda x: (-x[1], x[0]))<br>    return [w for w, _ in sorted_items[:3]]<br><br>top3_words('Hello world hello World foo bar foo bar baz')<br># ['bar', 'foo', 'hello']</code></pre><div class="source">Джерела: <a href="https://docs.python.org/3.14/library/re.html">Python 3.14: re</a>; <a href="https://docs.python.org/3.14/library/collections.html#collections.Counter">Python 3.14: Counter</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_011 ref::TV_PYTHON_CODING_16 source::PythonDocs source::Community
Реалізуйте anagram check для Unicode strings: застосуйте NFKC та <code>casefold()</code>, ігноруйте whitespace, а інші code points враховуйте.	<span class="key">Застосовуємо <code>unicodedata.normalize('NFKC', s)</code>, потім <code>.casefold()</code> для case-insensitive порівняння, фільтруємо <code>.isspace()</code> code points та порівнюємо <code>sorted()</code> списки символів.</span> NFKC нормалізує compatibility variants (наприклад, \u00e9 vs e\u0301), <code>casefold()</code> агресивніший за <code>.lower()</code> для Unicode (наприклад, німецьке \u00df \u2192 ss).<pre class="code-block"><code>import unicodedata<br><br>def is_anagram(s1, s2):<br>    def normalize(s):<br>        nfkc = unicodedata.normalize('NFKC', s)<br>        folded = nfkc.casefold()<br>        return sorted(ch for ch in folded if not ch.isspace())<br>    return normalize(s1) == normalize(s2)<br><br>is_anagram('Listen', 'Silent')  # True<br>is_anagram('\u00e9', 'e\u0301')  # True</code></pre><div class="source">Джерела: <a href="https://docs.python.org/3.14/library/unicodedata.html">Python 3.14: unicodedata</a>; <a href="https://docs.python.org/3.14/library/stdtypes.html#str.casefold">Python 3.14: str.casefold</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_012 ref::TV_PYTHON_CODING_26 source::PythonDocs source::Community
Реалізуйте streaming RLE encoder, який з iterable characters ліниво видає tuples <code>(character, positive_count)</code> з O(1) auxiliary memory; empty input не видає нічого.	<span class="key">Generator зберігає лише <code>current</code> character та <code>count</code>, yield при зміні character; empty input обробляється через <code>StopIteration</code> на <code>next(it)</code>.</span> O(1) memory досягається тим, що generator не матеріалізує input, а обробляє потік по одному елементу. <span class="warn">Не використовуйте <code>itertools.groupby</code> без розуміння, що він потребує попереднього сортування для не-суміжних дублікатів.</span><pre class="code-block"><code>def rle_encode(iterable):<br>    it = iter(iterable)<br>    try:<br>        current = next(it)<br>    except StopIteration:<br>        return<br>    count = 1<br>    for ch in it:<br>        if ch == current:<br>            count += 1<br>        else:<br>            yield (current, count)<br>            current = ch<br>            count = 1<br>    yield (current, count)<br><br>list(rle_encode('aaabbc'))  # [('a', 3), ('b', 2), ('c', 1)]<br>list(rle_encode(''))  # []</code></pre><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/expressions.html#generator-iterator-constructors">Python 3.14: generator expressions</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_013 ref::TV_PYTHON_CODING_25 source::PythonDocs source::Community
Перемістіть numeric zeros в кінець list in place, зберігши order інших elements та O(1) extra space; <code>False</code> не вважайте нулем.	<span class="key">Двох-показчиковий алгоритм: <code>pos</code> вказує на наступну позицію для non-zero, <code>i</code> сканує список; <code>isinstance(item, bool)</code> перевіряється перед <code>item != 0</code>, бо <code>False == 0</code> в Python.</span> <span class="warn">Уникайте <code>lst.sort(key=lambda x: x == 0)</code> — це не stable для bool та не зберігає порядок non-zero елементів.</span><pre class="code-block"><code>def move_zeros(lst):<br>    pos = 0<br>    for i in range(len(lst)):<br>        item = lst[i]<br>        if isinstance(item, bool) or item != 0:<br>            lst[pos], lst[i] = lst[i], lst[pos]<br>            pos += 1<br><br>a = [1, 0, 2, 0, 3, False, 0, 4]<br>move_zeros(a)  # [1, 2, 3, False, 4, 0, 0, 0]</code></pre><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/expressions.html#value-comparisons">Python 3.14: value comparisons</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_014 ref::TV_PYTHON_CODING_23 source::PythonDocs source::Community
Реалізуйте <code>fibonacci(n, first=0, second=1)</code>, що повертає новий lazy iterator для кожного виклику і видає рівно n values з O(1) state.	<span class="key">Внутрішній generator зберігає <code>a, b</code> як O(1) state та <code>range(n)</code> для лічильника; кожен виклик <code>fibonacci()</code> створює новий generator object.</span> Generator є lazy — значення обчислюються лише при ітерації. <span class="warn">Не використовуйте рекурсію без memoization — O(2^n) time complexity.</span><pre class="code-block"><code>def fibonacci(n, first=0, second=1):<br>    def gen():<br>        a, b = first, second<br>        for _ in range(n):<br>            yield a<br>            a, b = b, a + b<br>    return gen()<br><br>list(fibonacci(8))  # [0, 1, 1, 2, 3, 5, 8, 13]<br>list(fibonacci(0))  # []</code></pre><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/expressions.html#yield-expressions">Python 3.14: yield expressions</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_015 ref::TV_PYTHON_CODING_06 source::PythonDocs source::Community
Реалізуйте decorator для вимірювання elapsed time, який зберігає metadata, return value та exception behavior wrapped callable.	<span class="key"><code>functools.wraps</code> копіює metadata (<code>__name__</code>, <code>__doc__</code>, <code>__wrapped__</code>), <code>time.perf_counter()</code> для high-resolution elapsed time, return value передається через <code>return result</code>, exception propagation гарантується відсутністю <code>try/except</code> що ловить помилки.</span> <span class="warn"><code>time.time()</code> має нижчу точність та чутливий до system clock changes; <code>time.perf_counter()</code> є monotonic та має найвищу доступну точність.</span><pre class="code-block"><code>import time<br>import functools<br><br>def timed(func):<br>    @functools.wraps(func)<br>    def wrapper(*args, **kwargs):<br>        start = time.perf_counter()<br>        try:<br>            result = func(*args, **kwargs)<br>            return result<br>        finally:<br>            elapsed = time.perf_counter() - start<br>            wrapper.last_elapsed = elapsed<br>    wrapper.last_elapsed = None<br>    return wrapper<br><br>@timed<br>def slow_add(a, b):<br>    time.sleep(0.01)<br>    return a + b<br><br>slow_add(1, 2)  # 3<br>slow_add.__name__  # 'slow_add'</code></pre><div class="source">Джерела: <a href="https://docs.python.org/3.14/library/functools.html#functools.wraps">Python 3.14: functools.wraps</a>; <a href="https://docs.python.org/3.14/library/time.html#time.perf_counter">Python 3.14: time.perf_counter</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_016 ref::TV_PYTHON_CODING_09 source::PythonDocs source::Community
Спроєктуйте thread-safe rate-limit decorator з injected monotonic clock: до X викликів за rolling T seconds, перевищення викликає явний exception.	<span class="key"><code>threading.Lock</code> захищає <code>timestamps</code> list, injected <code>clock</code> (default <code>time.monotonic</code>) дозволяє тестування, rolling window реалізується видаленням expired timestamps з початку list.</span> <span class="warn"><code>time.time()</code> не підходить — не monotonic; <code>time.monotonic()</code> гарантує відсутність backward jumps. Race condition між <code>len(timestamps)</code> check та <code>timestamps.append()</code> вимагає lock.</span><pre class="code-block"><code>import threading<br>import time<br>import functools<br><br>class RateLimitExceeded(Exception):<br>    pass<br><br>def rate_limit(max_calls, period, clock=None):<br>    if clock is None:<br>        clock = time.monotonic<br>    def decorator(func):<br>        lock = threading.Lock()<br>        timestamps = []<br>        @functools.wraps(func)<br>        def wrapper(*args, **kwargs):<br>            now = clock()<br>            with lock:<br>                cutoff = now - period<br>                while timestamps and timestamps[0] &lt;= cutoff:<br>                    timestamps.pop(0)<br>                if len(timestamps) &gt;= max_calls:<br>                    raise RateLimitExceeded(<br>                        f"Rate limit: {max_calls} calls per {period}s"<br>                    )<br>                timestamps.append(now)<br>            return func(*args, **kwargs)<br>        return wrapper<br>    return decorator</code></pre><div class="source">Джерела: <a href="https://docs.python.org/3.14/library/threading.html#threading.Lock">Python 3.14: threading.Lock</a>; <a href="https://docs.python.org/3.14/library/time.html#time.monotonic">Python 3.14: time.monotonic</a></div>	topic::20_practical_coding type::Scenario level::Senior scope::Core card::PYI_20_017 ref::TV_PYTHON_CODING_24 source::PythonDocs source::Community
Реалізуйте context manager для transaction-like resource, який commit при success, rollback при exception та не приховує failure.	<span class="key"><code>__exit__</code> перевіряє <code>exc_type is None</code> для commit, інакше rollback; <code>return False</code> (або неявний <code>None</code>) не приховує exception — він propagate далі.</span> <span class="warn"><code>return True</code> з <code>__exit__</code> приховує exception — це небезпечно для transaction semantics, бо caller не дізнається про failure.</span><pre class="code-block"><code>class Transaction:<br>    def __init__(self, name):<br>        self.name = name<br>        self.committed = False<br>        self.rolled_back = False<br><br>    def __enter__(self):<br>        return self<br><br>    def __exit__(self, exc_type, exc_val, exc_tb):<br>        if exc_type is None:<br>            self.committed = True<br>        else:<br>            self.rolled_back = True<br>        return False  # do not suppress exception<br><br>t = Transaction("tx1")<br>with t:<br>    pass<br>t.committed  # True</code></pre><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/datamodel.html#with-statement-context-managers">Python 3.14: context managers</a></div>	topic::20_practical_coding type::Code level::Senior scope::Core card::PYI_20_018 ref::OFFICIAL source::PythonDocs
Реалізуйте validation descriptor, який зберігає значення окремо для кожного instance і працює з двома managed attributes.	<span class="key"><code>__set_name__</code> зберігає унікальний <code>attr_name</code> для кожного descriptor instance (наприклад, <code>_val_name</code>, <code>_val_age</code>), <code>__get__</code>/<code>__set__</code> використовують <code>getattr</code>/<code>setattr</code> з цим <code>attr_name</code> для per-instance storage.</span> <span class="warn">Class-level access (<code>User.name</code>) повертає сам descriptor — це дозволяє introspection та декоратори.</span><pre class="code-block"><code>class Validated:<br>    def __init__(self, validator):<br>        self.validator = validator<br>        self.attr_name = None<br><br>    def __set_name__(self, owner, name):<br>        self.attr_name = f'_val_{name}'<br><br>    def __get__(self, instance, owner=None):<br>        if instance is None:<br>            return self<br>        return getattr(instance, self.attr_name, None)<br><br>    def __set__(self, instance, value):<br>        self.validator(value)<br>        setattr(instance, self.attr_name, value)</code></pre><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/datamodel.html#implementing-descriptors">Python 3.14: descriptors</a></div>	topic::20_practical_coding type::Code level::Middle scope::Core card::PYI_20_019 ref::TV_PYTHON_CODING_18 source::PythonDocs source::Community
Спроєктуйте immutable value object з коректно узгодженими <code>__eq__</code> та <code>__hash__</code>, придатний для <code>dict</code> key.	<span class="key"><code>__slots__</code> запобігає створенню <code>__dict__</code>, <code>__setattr__</code> raise <code>AttributeError</code> для immutability, <code>__eq__</code> порівнює поля, <code>__hash__</code> повертає <code>hash(tuple_of_fields)</code> — гарантує <code>a == b</code> \u21d2 <code>hash(a) == hash(b)</code>.</span> <span class="warn">Якщо <code>__eq__</code> перевизначено без <code>__hash__</code>, Python автоматично встановлює <code>__hash__ = None</code> — об'єкт стане unhashable.</span><pre class="code-block"><code>class Point:<br>    __slots__ = ('_x', '_y')<br><br>    def __init__(self, x, y):<br>        object.__setattr__(self, '_x', x)<br>        object.__setattr__(self, '_y', y)<br><br>    @property<br>    def x(self):<br>        return self._x<br><br>    def __eq__(self, other):<br>        if not isinstance(other, Point):<br>            return NotImplemented<br>        return self._x == other._x and self._y == other._y<br><br>    def __hash__(self):<br>        return hash((self._x, self._y))<br><br>    def __setattr__(self, name, value):<br>        raise AttributeError("Point is immutable")<br><br>p1 = Point(1, 2)<br>p2 = Point(1, 2)<br>p1 == p2  # True<br>hash(p1) == hash(p2)  # True<br>d = {p1: "a"}<br>d[p2]  # "a"</code></pre><div class="source">Джерело: <a href="https://docs.python.org/3.14/reference/datamodel.html#object.__hash__">Python 3.14: object.__hash__</a></div>	topic::20_practical_coding type::Code level::Senior scope::Core card::PYI_20_020 ref::TV_PYTHON_CODING_17 source::PythonDocs source::Community
Для <code>is_prime(n)</code>, яка перевіряє divisors до <code>int(sqrt(n))</code>, складіть tests, що вбивають mutants <code>&lt;</code> ↔ <code>&lt;=</code> на square boundary і помилку для <code>n &lt; 2</code>.	<span class="key">Три групи тестів: base case <code>n &lt; 2</code>, perfect square (вбиває мутацію <code>&lt;</code> ↔ <code>&lt;=</code>) і звичайні prime/composite.</span> Для мутації: <code>is_prime(4)</code> — <code>int(sqrt(4)) == 2</code>; з правильним <code>&lt;=</code> divisor 2 знаходить <code>4 % 2 == 0</code> → <code>False</code>, а з <code>&lt;</code> діапазон порожній і повертає хибний <code>True</code>. Аналогічно <code>is_prime(9)</code> (sqrt=3). Для <code>n &lt; 2</code>: <code>is_prime(0)</code>, <code>is_prime(1)</code>, <code>is_prime(-7)</code> → <code>False</code>. Додатково: <code>is_prime(2)</code> → <code>True</code>, <code>is_prime(25)</code> → <code>False</code>.<div class="source">Джерела: <a href="https://docs.python.org/3.14/tutorial/">Python 3.14 Tutorial</a>; <a href="https://github.com/tavor118/pj_python_interview_questions_and_answers/blob/02d57a7a9f34fd386eb8aa5c0094fe3f3c3ba141/docs/python/coding.md">pj_python_interview_questions: is_prime</a></div>	topic::20_practical_coding type::Scenario level::Middle scope::Core card::PYI_20_021 ref::TV_PYTHON_CODING_04 source::PythonDocs source::Community
Складіть tests для Two Sum API, який повертає indices першої пари в lexicographic order, не використовує element двічі і повертає <code>None</code> без solution.	<span class="key">Tests перевіряють чотири властивості: сума пари дорівнює target, пара лексикографічно найменша, жоден індекс не використано двічі, <code>None</code> коли розв'язку немає.</span> Кейси: базовий <code>[2,7,11,15], target=9 → (0,1)</code>; заборона повтору — <code>[3,2,4], target=6 → (1,2)</code>, а не <code>(0,0)</code>; лексикографічна першість — за наявності кількох пар повертається найменша за <code>(i,j)</code>; <code>[1,2,3], target=100 → None</code>; edge cases: порожній список, від'ємні числа, дублікати значень.<div class="source">Джерела: <a href="https://docs.python.org/3.14/tutorial/">Python 3.14 Tutorial</a>; <a href="https://github.com/tavor118/pj_python_interview_questions_and_answers/blob/02d57a7a9f34fd386eb8aa5c0094fe3f3c3ba141/docs/python/coding.md">pj_python_interview_questions: two_sum</a></div>	topic::20_practical_coding type::Scenario level::Middle scope::Core card::PYI_20_022 ref::TV_PYTHON_CODING_27 source::PythonDocs source::Community
Складіть tests для maximum-overlapping-intervals function: інтервали closed <code>[start, end]</code>, тому start і end в одну мить вважаються overlapping.	<span class="key">Ключовий boundary — closed intervals: дотичні <code>[1,5]</code> і <code>[5,10]</code> дають overlap == 2 у точці 5.</span> Тести: дотик <code>[(1,5),(5,10)] → 2</code>; повна вкладеність <code>[(1,10),(2,9),(3,8)] → 3</code>; без перетину <code>[(1,3),(5,7)] → 1</code>; порожній список → 0; один інтервал → 1; усі однакові <code>[(1,5)]*4 → 4</code>; вироджений <code>[(3,3),(3,3)] → 2</code>.<div class="source">Джерела: <a href="https://docs.python.org/3.14/tutorial/">Python 3.14 Tutorial</a>; <a href="https://github.com/tavor118/pj_python_interview_questions_and_answers/blob/02d57a7a9f34fd386eb8aa5c0094fe3f3c3ba141/docs/python/coding.md">pj_python_interview_questions: max_overlap</a></div>	topic::20_practical_coding type::Scenario level::Middle scope::Core card::PYI_20_023 ref::TV_PYTHON_CODING_20 source::PythonDocs source::Community
Складіть boundary та round-trip tests для RLE decoder з grammar <code>count:character|count:character</code>: count є positive decimal, а character не може бути <code>:</code> чи <code>|</code>; malformed input викликає <code>ValueError</code>.	<span class="key">Boundary: мінімальний <code>count=1</code>, round-trip <code>decode(encode(s)) == s</code>; malformed → <code>ValueError</code> для заборонених символів, нульового count і порушення grammar.</span> Валідні: <code>"3:a|2:b|1:c" → "aaabbc"</code>; <code>"1:x" → "x"</code>; <code>"12:ab" → "ab" * 12</code>. Round-trip: <code>decode(encode("aabbc")) == "aabbc"</code>. Malformed → <code>ValueError</code>: <code>""</code> (порожній), <code>":a"</code> (немає count), <code>"0:a"</code> (count ≠ positive), <code>"-1:a"</code> (не decimal), <code>"1::"</code> (character=<code>:</code>), <code>"1:|"</code> (character=<code>|</code>), <code>"3:a|"</code> (trailing <code>|</code>), <code>"a:b"</code> (non-digit count).<div class="source">Джерела: <a href="https://docs.python.org/3.14/tutorial/">Python 3.14 Tutorial</a>; <a href="https://github.com/tavor118/pj_python_interview_questions_and_answers/blob/02d57a7a9f34fd386eb8aa5c0094fe3f3c3ba141/docs/python/coding.md">pj_python_interview_questions: RLE</a></div>	topic::20_practical_coding type::Scenario level::Middle scope::Core card::PYI_20_024 ref::TV_PYTHON_CODING_25 source::PythonDocs source::Community
