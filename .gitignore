# build output
/dist/
/site/dist/
/site/.astro/

# built Anki packages - binaries, published as GitHub Release assets, never committed
/anki/dist/

# node
node_modules/

# python
__pycache__/
*.py[cod]
.pytest_cache/
# meta/quality-gates.md tells you to run pytest with an explicit --basetemp; this is where
# that lands when the writable directory you pick is inside the repository
.pytest-basetemp/
.pytest-basetemp-*/
.venv/
*.egg-info/

# local review runs are temporary; accepted findings live in meta/review-backlog.md
/.audit-run-*/

# editors
.idea/
.vscode/
/.qwen/agents/
/.qwen/orchestration/

# local agent instructions are maintained and transferred by the owner
/AGENTS.md

# os
Thumbs.db
desktop.ini
.DS_Store

# spike run artifacts
/anki/spike/.tag-refresh-run-*/
/anki/spike/.tag-refresh-inspect.anki2
/anki/spike/.tag-refresh-venv/
/anki/spike/.tag-refresh-inspect.media/

# generated mirror for the site
/site/src/content/docs/

# generated Starlight sidebar (tools/iqa/mirror.py), like the mirror itself
/site/src/generated/
