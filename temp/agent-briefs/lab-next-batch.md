# Bounded continuation: Embedded Interview Lab import

Work in `C:\Users\bogdan\Desktop\interview-qa`.

Read first: `AGENTS.md`, `meta/QUESTIONS.md`, `meta/TAXONOMY.md`, and the existing files under `content/uk/embedded/data-types-and-memory-layout` plus `content/uk/embedded/c-in-embedded`.

Continue the interrupted import from `C:\Users\bogdan\Documents\Projects\learning\Embeddedinterviewlab`. The previous worker completed all 100 cards in `01_Data_Types_Memory_Layout_anki_cards.txt`, using ids `emb-dtypes-0001` through `emb-dtypes-0100`. Do not redo those cards. Import exactly the next 20 source cards from `02_Pointers_Arrays_anki_cards.txt`, source cards 1–20, into the project taxonomy section `embedded/c-in-embedded`.

For each source card, preserve the source Front as the Ukrainian question and the source Back as the Ukrainian Short answer, adapted only to the repository Markdown/HTML style. Create the matching English file with the same question id, translated title/description as appropriate but keep the whole English body `TODO`. Keep Ukrainian Detailed explanation and every other not-yet-written section `TODO`. Use the existing imported embedded files as the exact structural/style pattern. Keep one question in one place and do not invent content.

The owner explicitly accepted these temporary import gates: source is community-only, Short answer may be outside the 2–5 sentence limit, and ids are not yet added to `meta/id-registry.csv`. Do not edit the registry, taxonomy, validators, plans, existing questions, or another worker's files. Do not add authoritative sources or shorten answers in this batch. Do not commit.

Use unique ids continuing the Lab sequence: `emb-cppfound-0001` through `emb-cppfound-0020` unless existing files prove a collision; in that case stop and report the collision instead of reusing an id. Preserve source order in filenames and metadata. Avoid U+2014, U+2190, and U+2192 in written content.

After writing the 20 Ukrainian/English pairs, run a focused check over only the new files for matching ids, required sections, English TODO bodies, and forbidden characters. Do not treat the repository-wide validator's expected import-gate failures as a reason to alter unrelated files.

Write the concise report to `temp/agent-reports/lab-next-batch.md` with files, ids, counts, checks, and any judgment calls. Stop after this one 20-card batch, even if more source cards remain.
