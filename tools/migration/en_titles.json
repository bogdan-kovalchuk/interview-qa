{
"PYI_01_001": "What stages does a Python module go through from source code to executing runtime instructions, and at which stage can bytecode appear?",
"PYI_01_002": "Why does splitting languages into just \"compiled\" and \"interpreted\" incorrectly describe Python?",
"PYI_01_003": "What problem does the cached `.pyc` file solve, and why does its presence not mean the Python code was compiled to native machine code?",
"PYI_01_004": "What exactly is dynamic about Python's dynamic typing: the type of an object, or the binding of a name to an object?",
"PYI_01_005": "Why are dynamic typing and strong typing not opposite characteristics of a language?",
"PYI_01_006": "How do you tell a language guarantee of Python apart from an implementation detail, using memory management or bytecode as an example?",
"PYI_01_007": "What risks arise if code relies on CPython-specific behaviour when porting to PyPy or another Python implementation?",
"PYI_01_008": "A plugin receives a callback name as a string: when is it appropriate to use `getattr(plugin, name)`, and when is an explicit registry of allowed callbacks a safer API?",
"PYI_01_009": "How does reflection differ from ordinary access to an object's attribute that is already known in advance?",
"PYI_01_010": "When can monkey patching be justified, and what risks does it create for the locality of changes, tests, and dependency upgrades?",
"PYI_01_011": "Why can automatically loading plugin classes through reflection be worse than an explicit registry, even though it removes manual registration?",
"PYI_01_012": "How does the executable nature of `def` and `class` statements affect conditional definition, redefinition, and import-time side effects?",

"PYI_02_001": "Do the `and` and `or` operators always return a `bool`, or one of their own operands, and how does short-circuit evaluation work?",
"PYI_02_002": "What value does the expression `[] or \"fallback\"` produce, and why is the result not `True` or `False`?",
"PYI_02_003": "Why can the construct `value = user_input or default` incorrectly replace a valid value of `0` or an empty string?",
"PYI_02_004": "How does extended iterable unpacking work in the assignment `head, *middle, tail = items`, and what happens if there are not enough elements?",
"PYI_02_005": "How does the assignment expression `:=` differ from an ordinary assignment, and when does using it hurt readability?",
"PYI_02_006": "How does evaluation order affect the result of a chained assignment or unpacking when the right-hand expression has side effects?",
"PYI_02_007": "When does the `else` block of a `for` or `while` loop run, and how does `break` change that behaviour?",
"PYI_02_008": "Why doesn't `continue` inside a `try` cancel the execution of the corresponding `finally` before the next iteration?",
"PYI_02_009": "How does a capture pattern in `match` differ from a value pattern, and why can an unqualified name unexpectedly bind a new variable?",
"PYI_02_010": "When is the guard in `case ... if condition` evaluated, and does a failed guard undo the bindings the pattern itself created?",
"PYI_02_011": "Why does the order of `case` clauses in structural pattern matching affect the reachability of the patterns that follow?",
"PYI_02_012": "In what order does Python evaluate the function expression, positional arguments, and keyword arguments in a call, when they have side effects?",
"PYI_02_013": "Why does the expression `-3 ** 2` give a different result than `(-3) ** 2`?",
"PYI_02_014": "How does the chained comparison `a < b < c` differ from `a < b and b < c` in the number of times the middle operand is evaluated?",

"PYI_03_001": "How does an object-identity check with `is` differ from equality with `==`?",
"PYI_03_002": "Why is `is None` used to check for `None` instead of relying on `== None`?",
"PYI_03_003": "Why can't the result of an identity comparison between two identical literals be used as proof of interning or as a portable Python guarantee?",
"PYI_03_004": "What consistency requirements must `__eq__` and `__hash__` satisfy if instances are used as dictionary keys?",
"PYI_03_005": "Why doesn't `y = x` copy a mutable object, and how does a mutation through `y` become visible through `x`?",
"PYI_03_006": "How does in-place mutation of a list via `+=` differ from augmented assignment for an immutable tuple?",
"PYI_03_007": "Why can a tuple be unhashable even though the tuple itself is immutable?",
"PYI_03_008": "How can mutating an object after it has been used as a key break the logic of a hash-based collection?",
"PYI_03_009": "How does a shallow copy differ from a deep copy for a container with nested mutable objects?",
"PYI_03_010": "Why does `copy.deepcopy()` use a memo, and how does that help with shared references or cyclic structures?",
"PYI_03_011": "When should a custom class implement `__copy__` or `__deepcopy__` instead of relying on the default behaviour?",
"PYI_03_012": "Why shouldn't a mutable class with a value-based `__eq__` usually inherit the identity-based hash from `object`?",
"PYI_03_013": "How does hashability differ from immutability, and why aren't these properties full synonyms?",
"PYI_03_014": "Why can `0.1 + 0.2 == 0.3` be `False`, and how should approximate floating-point results be compared correctly?",
"PYI_03_015": "When is `Decimal` preferable to `float`, and what problem does it not solve automatically without the right precision and rounding policy?",
"PYI_03_016": "How does numeric equality between `1`, `1.0`, and `True` affect their use as keys of the same dictionary?",
"PYI_03_017": "How does `str` differ from `bytes`, and at which boundary of a program should encode/decode happen?",
"PYI_03_018": "When is `bytearray` preferable to `bytes`, and what trade-off does its mutability create?",
"PYI_03_019": "What copying problem does `memoryview` solve, and why does the lifetime of the underlying buffer object matter?",
"PYI_03_020": "Does a type annotation change an object's actual runtime type, or automatically forbid assigning a value of a different type?",
"PYI_03_021": "When is `isinstance()` an appropriate runtime check, and when does a protocol or duck typing make code less coupled to concrete classes?",
"PYI_03_022": "How does a parameterized static type such as `list[str]` differ from a runtime check of a specific list's contents?",

"PYI_04_001": "When is a tuple the better choice over a list, not because of syntax but because of the semantics of an immutable data structure?",
"PYI_04_002": "Why can `range` represent a very large range without storing all the integers in memory?",
"PYI_04_003": "What is the difference between `list.sort()` and `sorted()` regarding mutation, return value, and the input iterables each allows?",
"PYI_04_004": "Why can repeated concatenation of a list in a loop be worse than `append`/`extend`, even when the result is the same?",
"PYI_04_005": "How does a tuple containing a mutable element behave when `t[0] += value` is attempted, and why can the mutation happen before the `TypeError`?",
"PYI_04_006": "What exactly does dictionary insertion order guarantee in modern Python, and what does that guarantee not say about the internal hash table?",
"PYI_04_007": "Why must dictionary keys be hashable, and how does a dictionary use hash together with equality?",
"PYI_04_008": "How do `d[key]`, `d.get(key)`, and `setdefault()` differ in behaviour when the key is missing, and in their possible side effects?",
"PYI_04_009": "When is `collections.defaultdict` better than a manual key check, and what mistake can a wrong default factory hide?",
"PYI_04_010": "Why can changing the size of a dictionary while iterating its keys fail, while changing the value of an existing key is often allowed?",
"PYI_04_011": "How do the dict union operators `|` and `|=` resolve conflicts between identical keys, and how do they differ in mutation?",
"PYI_04_012": "What consequences does a custom `dict.__missing__()` have, and why isn't it called the same way by every method of accessing a key?",
"PYI_04_013": "When is a `set` better than a list for membership checks, and what element order should never be relied on as a contract?",
"PYI_04_014": "How does `set` differ from `frozenset` in mutability and in the ability to be an element of another set?",
"PYI_04_015": "How do set operations let you express a subset/superset check without manual nested loops?",
"PYI_04_016": "Why can an incorrect `__eq__` on custom objects lead to unexpected results for set membership?",
"PYI_04_017": "Are `dict.keys()`, `dict.values()`, and `dict.items()` snapshots, and how does their live-view nature affect later changes to the dictionary?",
"PYI_04_018": "What should a custom collection's `__iter__` return, and why is returning the reusable container itself usually incorrect?",
"PYI_04_019": "How does sequence comparison work lexicographically, and what happens at the first pair of elements that do not support ordering?",
"PYI_04_020": "What does the stability of Python's sort mean, and how can it be used for multi-key sorting without a custom comparator?",
"PYI_04_021": "When is a key function in `sorted()` better than implementing rich comparison methods on a domain class?",
"PYI_04_022": "Why does membership in a list usually cost linear time, while a set or dict is expected to give average constant-time lookup?",
"PYI_04_023": "When is `collections.deque` better than a list for working at both ends of a sequence?",
"PYI_04_024": "Why can hash flooding or a large number of collisions ruin the expected O(1) lookup, and why doesn't that change the dictionary's logical contract?",

"PYI_05_001": "What different constraints on how arguments may be passed do the `/` and `*` symbols set in the signature `def f(a, /, b, *, c): ...`?",
"PYI_05_002": "When and why does the call `f(1, a=2)` for `def f(a): ...` fail: before entering the function body, or while it runs?",
"PYI_05_003": "How do you forward received `*args` and `**kwargs` to another function without losing positional and keyword arguments, and what mistake is easy to make while forwarding them?",
"PYI_05_004": "How does Python bind the arguments in the call `f(1, 2, 3, b=4, x=5)` to the parameters of `def f(a, *args, b=0, **kwargs): ...`?",
"PYI_05_005": "Why can a wrapper with the signature `(*args, **kwargs)` accept a syntactically valid call that the wrapped function then rejects, and at what point does the `TypeError` occur?",
"PYI_05_006": "What do two consecutive calls `append(1)` and `append(2)` return for `def append(x, items=[]): items.append(x); return items`, and why?",
"PYI_05_007": "When is a default argument expression evaluated, and why might a default like `timeout=get_timeout()` fail to reflect later configuration changes?",
"PYI_05_008": "When is a custom sentinel object better than `None` as a default argument in a public API?",
"PYI_05_009": "Why doesn't the assignment `items = []` inside a function change the caller's list, while `items.append(value)` can change it?",
"PYI_05_010": "Why is the model \"Python passes arguments by reference\" inaccurate, and which model correctly explains both rebinding and mutation of a parameter?",
"PYI_05_011": "A function received a shallow copy of a list of dictionaries and changed a value in a nested dictionary. Why can the change be visible to the caller even though the outer list was copied?",
"PYI_05_012": "Why can a function get an `UnboundLocalError` when reading a global `x`, if there is an assignment `x = ...` further down in its body?",
"PYI_05_013": "How do `global` and `nonlocal` differ in which scope they look for a binding to assign to?",
"PYI_05_014": "How will Python look up the name `len` inside a function if there is no local binding, but the module defines its own `len`?",
"PYI_05_015": "Why doesn't a name defined in a class body become an ordinary enclosing lexical scope for the code of that class's methods?",
"PYI_05_016": "Why do all the functions created as `[lambda: i for i in range(3)]` return the same value once the comprehension has finished?",
"PYI_05_017": "Why does writing `lambda i=i: i` remove the late-binding problem in a loop, even though default arguments can themselves be a source of bugs?",
"PYI_05_018": "How does `nonlocal` let a closure mutate captured state, and what risk does that create under concurrent calls?",
"PYI_05_019": "Why can an object captured by a closure stay alive after the enclosing function has finished, and how does that affect memory retention?",
"PYI_05_020": "How does passing `handler` differ from passing `handler()` as an argument to a higher-order function?",
"PYI_05_021": "When is a named function via `def` a better choice than a `lambda`, even when both can implement the same short expression?",
"PYI_05_022": "When is a callable object with `__call__` preferable to a closure for encapsulating a functional component's state?",
"PYI_05_023": "Do type annotations change the runtime semantics of calling an ordinary Python function, and does the interpreter check argument types automatically?",
"PYI_05_024": "How did the standard semantics of evaluating annotations change in Python 3.14, and why should `annotationlib` be used for inspecting them safely instead of relying only on reading `__annotations__` directly?",

"PYI_06_001": "How does `__new__` differ from `__init__` in its role in creating an instance and in its return value?",
"PYI_06_002": "Why does customizing an immutable subclass such as a custom `tuple` require `__new__`, while changes made only in `__init__` come too late?",
"PYI_06_003": "What happens if `__init__` explicitly returns a value other than `None`, and why?",
"PYI_06_004": "In what order does Python look up an instance attribute when a data descriptor, instance dictionary, non-data descriptor, and class attribute are all present?",
"PYI_06_005": "How does `__getattribute__` differ from `__getattr__`, and when is each one called?",
"PYI_06_006": "What does name mangling actually do for an attribute named `__value`, and why is it not a privacy security mechanism?",
"PYI_06_007": "Why can a property usually not be shadowed by a same-named value in `instance.__dict__`?",
"PYI_06_008": "How does the absence of `__dict__` in a class with `__slots__` affect dynamically adding attributes?",
"PYI_06_009": "How does a data descriptor differ from a non-data descriptor, and how does that difference change precedence during attribute lookup?",
"PYI_06_010": "Which methods make up the descriptor protocol, and what attribute-encapsulation problem does it solve?",
"PYI_06_011": "Why can `property` be seen as a descriptor, and when is it better than explicit `get_*`/`set_*` methods?",
"PYI_06_012": "How should a reusable validation descriptor store values separately for each instance, without creating shared state between objects?",
"PYI_06_013": "Why does accessing `obj.method` create a bound method, and where does the implicit `self` come from at call time?",
"PYI_06_014": "When should you choose an instance method, `@classmethod`, or `@staticmethod`, given the binding and polymorphism you need?",
"PYI_06_015": "How does the function descriptor turn a function into a bound method when accessed through an instance, but not when accessed through the class?",
"PYI_06_016": "How does C3 MRO determine the order in which methods are looked up under multiple inheritance, and what ordering property does it preserve?",
"PYI_06_017": "Why does zero-argument `super()` mean \"the next one in the MRO\" rather than simply \"the immediate parent class\"?",
"PYI_06_018": "How does Python resolve diamond inheritance, and why can direct calls to specific parent methods run one link twice?",
"PYI_06_019": "What rules for the signature and calling of `super()` must cooperative methods follow in a multiple-inheritance hierarchy?",
"PYI_06_020": "What properties make a mixin safe for multiple inheritance, and why does a mixin usually not own the object's primary state?",
"PYI_06_021": "How does `__repr__` differ from `__str__` in intended audience and fallback behaviour?",
"PYI_06_022": "How can `__len__` affect an object's truthiness in the absence of `__bool__`?",
"PYI_06_023": "Why should a comparison method return `NotImplemented` for an unsupported operand instead of an immediate `False` or an exception?",
"PYI_06_024": "How does overriding `__eq__` affect the inherited `__hash__` and the use of an instance in a set or dict?",
"PYI_06_025": "When is an abstract base class more appropriate than an informal duck-typing check?",
"PYI_06_026": "How does structural subtyping via `typing.Protocol` differ from nominal inheritance via an ABC?",
"PYI_06_027": "How do the `eq`, `frozen`, and `unsafe_hash` parameters affect the generation of `__eq__` and `__hash__` for a `@dataclass`?",
"PYI_06_028": "What memory and inheritance trade-offs does `__slots__` have, and why is it not a universal performance optimisation?",
"PYI_06_029": "Why is a class an instance of the metaclass `type`, and what main stages does a class statement go through before the class object is created?",
"PYI_06_030": "When is a metaclass actually justified, and when does a class decorator or `__init_subclass__` give a simpler solution?",
"PYI_06_031": "Why does `frozen=True` on a `@dataclass` forbid reassigning fields, but not make a nested mutable object immutable?",

"PYI_07_001": "When is the decorator expression evaluated, and when is the wrapper the decorator returned actually called?",
"PYI_07_002": "In what order are two decorators, `@outer` and `@inner`, written above the same function, applied?",
"PYI_07_003": "What metadata, introspection, and debugging problems arise if a wrapper does not use `functools.wraps`?",
"PYI_07_004": "What does `functools.wraps` not guarantee about the wrapper function's actual call signature?",
"PYI_07_005": "How does a decorator factory differ from the decorator itself, and at what stage are its arguments processed?",
"PYI_07_006": "How do you design a decorator with two distinct call forms, `@trace` and `@trace(level=2)`, without confusing the decorated callable with configuration arguments?",
"PYI_07_007": "When is a callable class a better implementation of a stateful decorator than a closure?",
"PYI_07_008": "How does a class decorator differ from a metaclass in the timing and scope of its effect on the class object?",
"PYI_07_009": "How does the order of stacking decorators affect authorization, caching, and logging, if any wrapper may choose not to call the next one?",
"PYI_07_010": "Why can state in a closure-based decorator create a race condition under concurrent calls to the wrapped function?",
"PYI_07_011": "How should a decorator for `async def` preserve awaitability and the correct propagation of exceptions?",
"PYI_07_012": "What happens to the decorated name if the decorator returns `None` instead of a callable?",

"PYI_08_001": "How does an iterable differ from an iterator, and what methods make up their respective protocols?",
"PYI_08_002": "Why does `iter(container)` usually return a new iterator, while `iter(iterator)` must return the same object?",
"PYI_08_003": "How should a custom iterator correctly signal that it is finished, and why is returning a sentinel value from `__next__` not equivalent?",
"PYI_08_004": "How do you implement a re-iterable collection so that two nested iterations do not share one cursor?",
"PYI_08_005": "When does the body of a generator function run: when the function is called, or on the first `next()`?",
"PYI_08_006": "What state does a suspended generator keep between two `yield`s, and what happens after it finishes?",
"PYI_08_007": "Why can a generator pipeline reduce peak memory but not necessarily total execution time?",
"PYI_08_008": "How do side effects in a generator change when exceptions occur, compared to eager list construction?",
"PYI_08_009": "What does the expression `yield from subiterator` delegate beyond simply re-yielding each value?",
"PYI_08_010": "How is a subgenerator's `return` value passed to the delegating generator through `yield from`?",
"PYI_08_011": "Under what condition is the first call to `generator.send(value)` allowed, and why is `None` usually sent first?",
"PYI_08_012": "How do `throw()` and `close()` affect control flow inside a suspended generator?",
"PYI_08_013": "Why doesn't iterating an already exhausted generator again start the computation over?",
"PYI_08_014": "What problem does passing a single iterator to two consumers that read it in turn create?",
"PYI_08_015": "Why shouldn't you rely on garbage collection of a generator for the timely release of an external resource?",
"PYI_08_016": "When is a generator expression better than a list comprehension, and when does reuse or indexing make a list more appropriate?",
"PYI_08_017": "In what scope is a generator expression's outermost iterable evaluated, and when are its remaining clauses evaluated?",
"PYI_08_018": "How does `itertools.tee()` let you create independent iterators, and what memory trade-off arises if the consumers advance at different speeds?",

"PYI_09_001": "What values does `__exit__` receive on normal completion versus on an exception inside the `with` block?",
"PYI_09_002": "What does a truthy return value from `__exit__` mean, and why is an accidental `return True` dangerous?",
"PYI_09_003": "How do you guarantee cleanup in `__exit__` without masking the original exception with a new error from the cleanup code?",
"PYI_09_004": "How does a class-based context manager differ from a generator-based manager built with `@contextmanager`?",
"PYI_09_005": "Why must a generator used with `@contextmanager` execute exactly one `yield`?",
"PYI_09_006": "When is `contextlib.ExitStack` better than statically nested `with` blocks?",
"PYI_09_007": "How does `ExitStack` preserve LIFO cleanup order, and what should happen if acquiring one of the resources fails?",
"PYI_09_008": "How do `async with` and the `__aenter__`/`__aexit__` methods differ from the synchronous context-manager protocol?",
"PYI_09_009": "How should `__aexit__` finish cleanup after a `CancelledError` without swallowing the task's cancellation?",
"PYI_09_010": "How does a reusable context manager differ from a reentrant context manager?",

"PYI_10_001": "Why is `except Exception` usually safer than a bare `except` for application-level handling?",
"PYI_10_002": "How does the order of several `except` clauses affect the reachability of handlers for subclass exceptions?",
"PYI_10_003": "When can catching `BaseException` break graceful shutdown or cancellation semantics?",
"PYI_10_004": "When does the `else` clause of a `try/except/else/finally` construct run, and why put success-path code there?",
"PYI_10_005": "In Python 3.14, what happens to a pending `return` from a `try` if `finally` also executes a `return`, and why does the compiler now emit a `SyntaxWarning` for that control flow?",
"PYI_10_006": "How can an exception in `finally` mask the original exception, and how do you preserve the diagnostic context?",
"PYI_10_007": "How does implicit exception chaining differ from an explicit `raise NewError(...) from original`?",
"PYI_10_008": "When is a bare `raise` used inside a handler, and why can `raise exc` change how the traceback is presented?",
"PYI_10_009": "How does `raise ... from None` affect how the exception context is displayed, and when is that justified?",
"PYI_10_010": "What properties make a custom exception hierarchy a stable part of a library API?",
"PYI_10_011": "Why should a custom exception usually inherit from `Exception` rather than `BaseException`?",
"PYI_10_012": "What problem do `ExceptionGroup` and `except*` solve in concurrent code?",
"PYI_10_013": "Why can a single `except*` handle only a subgroup of exceptions, while the rest of the group continues to propagate?",
"PYI_10_014": "When is EAFP preferable to LBYL, and how can a race between the check and the action make LBYL incorrect?",
"PYI_10_015": "When should a library emit a warning instead of raising an exception?",
"PYI_10_016": "How do you log an exception with its traceback without thoughtlessly duplicating it at every level of the call stack?",

"PYI_11_001": "What stages does `import package.module` go through, from finding the spec to executing the module code?",
"PYI_11_002": "What role does `sys.modules` play when the same module name is imported again?",
"PYI_11_003": "Why don't two `import module` statements usually run the module's top-level code twice, yet they can create different local bindings?",
"PYI_11_004": "How can violating the import protocol or manually editing `sys.modules` lead to two module objects for the same source file?",
"PYI_11_005": "In what order does Python search for a module through the import machinery and `sys.path`?",
"PYI_11_006": "When is an absolute import preferable to an explicit relative import inside a package?",
"PYI_11_007": "Why can a relative import fail when a package module is run as an ordinary script file?",
"PYI_11_008": "What does a partially initialized module mean during a circular import, and why aren't all of its names already available?",
"PYI_11_009": "The modules `orders` and `payments` import each other for the sake of a shared protocol: which dependency-direction refactoring removes the cycle better than a local import?",
"PYI_11_010": "How does a regular package with `__init__.py` differ from a namespace package?",
"PYI_11_011": "What tasks are appropriate to perform in a package's `__init__.py`, and which ones create unwanted import-time side effects?",
"PYI_11_012": "What exactly does `if __name__ == \"__main__\"` protect, and what does it not change about import semantics?",
"PYI_11_013": "Why doesn't `importlib.reload(module)` automatically update all previously created instances and names imported via `from ... import ...`?",
"PYI_11_014": "How does an import package/module differ from an installable distribution project with build metadata?",

"PYI_12_001": "Where is the boundary between `str` and `bytes` in text I/O, and where should encoding and decoding happen?",
"PYI_12_002": "Why is it worth setting `encoding` explicitly for a portable text file, instead of relying on the platform default?",
"PYI_12_003": "How can newline translation in text mode change the characters read or written on different platforms?",
"PYI_12_004": "How does a Python stream's `flush()` differ from `os.fsync()`, and why shouldn't either be described as a universal hardware-durability guarantee?",
"PYI_12_005": "What data can remain in a Python buffered stream after `write()`, and what does `flush()` change?",
"PYI_12_006": "Why is `with open(...)` more reliable than a manual `close()` after file operations?",
"PYI_12_007": "How do you update a file within the same filesystem via a temporary file and `os.replace()`, so a reader never sees partial content?",
"PYI_12_008": "How are `seek()` and `tell()` used for random access, and why can't text-stream offsets always be treated as character positions?",
"PYI_12_009": "How does `pathlib.Path` make building and traversing paths easier compared to manual string concatenation?",
"PYI_12_010": "Why doesn't a string-prefix check determine path containment when `..` is present, and how does `Path.resolve()` change the lexical path before comparison?",
"PYI_12_011": "How do you set up custom JSON encoding for an unsupported type without losing an explicit wire schema?",
"PYI_12_012": "Why doesn't a JSON round trip automatically preserve every Python type and object identity?",
"PYI_12_013": "Why can't `pickle.loads()` be called on untrusted data, even after checking the expected object type?",
"PYI_12_014": "When should you choose `StringIO`, and when `BytesIO`, for an in-memory stream?",

"PYI_13_001": "What scope does the iteration variable of a list comprehension have in Python 3, and does it replace a same-named outer variable?",
"PYI_13_002": "In what order are the clauses of a comprehension with multiple `for` and `if` evaluated?",
"PYI_13_003": "Why doesn't a comprehension's iteration variable leak into the containing scope, while a walrus assignment expression inside a comprehension can bind in the containing scope?",
"PYI_13_004": "Why must the groups returned by `itertools.groupby()` be consumed or materialized before moving on to the next group?",
"PYI_13_005": "At what point does a generator expression evaluate its elements, and how does that affect exceptions and side effects?",
"PYI_13_006": "Why can `map()` and `filter()` be exhausted in Python 3, and what happens after the first full pass over them?",
"PYI_13_007": "How do you build a lazy pipeline with `itertools` so intermediate collections are never materialized?",
"PYI_13_008": "The function `retry(operation)` must accept behaviour from the outside: how does treating it as a first-class function let you pass the operation without calling it immediately?",
"PYI_13_009": "How does `functools.partial` change the call interface without immediately calling the wrapped callable?",
"PYI_13_010": "When is `singledispatch` better than an extensible `if/elif` chain, and on which argument does it choose the implementation?",
"PYI_13_011": "When is a comprehension usually more readable than `map()` or `filter()`, and when does callable composition win?",
"PYI_13_012": "How does the initializer determine the result of `functools.reduce()` for an empty iterable, and how does left-to-right grouping change the result of a non-associative operation?",

"PYI_14_001": "How does CPython turn source code into a code object and bytecode before execution?",
"PYI_14_002": "What does an execution frame store in CPython, and how is it connected to the code object and the call stack?",
"PYI_14_003": "Why should `dis` output be treated as an implementation detail of a specific CPython version rather than a language contract?",
"PYI_14_004": "What role do adaptive bytecode and specialization play in CPython 3.14 while running hot code?",
"PYI_14_005": "Why can a retained traceback in CPython keep frames and their local objects alive after the function has finished?",
"PYI_14_006": "How does the reference count usually determine the moment of deallocation in GIL-enabled CPython 3.14, and why doesn't that describe the free-threaded build?",
"PYI_14_007": "Why does `sys.getrefcount(obj)` usually show at least one temporary reference more than expected?",
"PYI_14_008": "Why can't relying on immediate deallocation in CPython be treated as a portable Python language guarantee?",
"PYI_14_009": "Why can't reference counting by itself free an unreachable reference cycle?",
"PYI_14_010": "How does CPython's cyclic garbage collector detect unreachable container cycles on top of reference counting?",
"PYI_14_011": "When can `gc.disable()` be justified, and what measurements and invariants does that change require beforehand?",
"PYI_14_012": "What is the difference between a Python object's lifetime and the process allocator returning memory to the operating system?",
"PYI_14_013": "Why might a process's resident memory not shrink immediately after deleting a large number of Python objects?",
"PYI_14_014": "Why shouldn't `__del__` be used as the primary mechanism for the timely release of external resources?",
"PYI_14_015": "In free-threaded CPython 3.14, `sys.getrefcount()` for an object returns an unexpectedly large value: how does immortalization explain this, and why shouldn't that value be read as an exact reference count?",
"PYI_14_016": "When can `gc.freeze()` reduce copy-on-write losses before `fork()`, and what conditions does this pattern need?",
"PYI_14_017": "How does `dis` help you check which operations the compiler generates for a small function?",
"PYI_14_018": "How does `sys.getsizeof()` differ from a recursive measurement of the whole object graph?",
"PYI_14_019": "How does comparing `tracemalloc` snapshots help find the source locations of growing allocations?",
"PYI_14_020": "What conclusions about CPython's memory management are unsafe to carry over to PyPy or another Python implementation?",

"PYI_15_001": "How does a thread differ from a process in address space, resource sharing, and failure isolation?",
"PYI_15_002": "Which data do threads within one process see in common, and which must they keep separate?",
"PYI_15_003": "How does process isolation change the model for exchanging data and handling a worker crash, compared to threads?",
"PYI_15_004": "What exactly does the GIL serialize in an ordinary GIL-enabled build of CPython?",
"PYI_15_005": "Why can I/O-bound threads still give a concurrency benefit in GIL-enabled CPython?",
"PYI_15_006": "Why doesn't the GIL eliminate race conditions in Python application code?",
"PYI_15_007": "Why isn't the observed atomicity of a particular built-in operation in the current CPython a stable synchronization contract?",
"PYI_15_008": "What does the free-threaded build of CPython 3.14 mean, and how does it coexist with the GIL-enabled build?",
"PYI_15_009": "Which thread-safety assumptions about shared mutable objects need to be revisited when moving to free-threaded CPython?",
"PYI_15_010": "How does a C extension explicitly declare support for free-threaded CPython 3.14, and what can happen when an incompatible extension is imported?",
"PYI_15_011": "When is `ThreadPoolExecutor` an appropriate choice for an I/O-bound workload?",
"PYI_15_012": "When can `ProcessPoolExecutor` pay off for a CPU-bound pure-Python workload in GIL-enabled CPython?",
"PYI_15_013": "Why can parallelizing small CPU tasks across processes be slower than sequential execution?",
"PYI_15_014": "What is a race condition on a read-modify-write sequence, and why is a synchronization primitive needed?",
"PYI_15_015": "How do `Lock`, `RLock`, `Semaphore`, and `Condition` differ in synchronization intent?",
"PYI_15_016": "What conditions create a deadlock, and how does lock ordering reduce the risk?",
"PYI_15_017": "When one thread has to hand a sequence of jobs and results to another, why does `queue.Queue` give a clearer hand-off contract than a shared list without a lock?",
"PYI_15_018": "How do you choose between `ThreadPoolExecutor` and `ProcessPoolExecutor` based on workload, data-transfer cost, and failure model?",
"PYI_15_019": "How do you correctly collect exceptions and results from executor futures without losing a worker failure?",
"PYI_15_020": "In Python 3.14, which process start method is the default on POSIX and on Windows, and when do you need to explicitly choose `spawn`, `fork`, or `forkserver`?",
"PYI_15_021": "When should you choose a queue or a pipe for IPC, and when does shared memory justify the extra synchronization complexity?",
"PYI_15_022": "When is `threading.local()` better than a shared dictionary keyed by thread ID, and when does it hide unwanted implicit state?",

"PYI_16_001": "How do a coroutine function, a coroutine object, and the result of `await` differ from each other?",
"PYI_16_002": "What objects can be awaited, and how are coroutines, Tasks, and Futures related through the awaitable protocol?",
"PYI_16_003": "Why doesn't simply creating several coroutine objects already run them concurrently?",
"PYI_16_004": "Why does a fire-and-forget Task need a strong reference kept and its outcome explicitly handled?",
"PYI_16_005": "What makes an asyncio Task or callback ready after I/O, a timer, or a suspension point, and which fairness and exact-ordering guarantees can application code not assume?",
"PYI_16_006": "Why can a coroutine with no suspension point monopolize the event-loop thread?",
"PYI_16_007": "How does one blocking call inside `async def` affect the latency of every other Task on the same loop?",
"PYI_16_008": "How can a CPU-heavy coroutine or an unbounded ready queue cause starvation even in a cooperative scheduler?",
"PYI_16_009": "How does `asyncio.TaskGroup` guarantee that the Tasks created in it finish before the context manager exits?",
"PYI_16_010": "What happens to sibling Tasks and their exceptions when one Task in a `TaskGroup` fails?",
"PYI_16_011": "When does structured concurrency via `TaskGroup` give more reliable lifetime and failure semantics than a loose set of `create_task()` calls?",
"PYI_16_012": "How does `asyncio.gather()` preserve the ordering of results when the awaitables finish in a different order?",
"PYI_16_013": "When should you choose `wait()`, and when `as_completed()`, for handling a set of concurrent operations?",
"PYI_16_014": "What failure and cancellation semantics of `gather()` must be taken into account so sibling work isn't left unsupervised?",
"PYI_16_015": "How is a cancellation request delivered to a Task as a `CancelledError`, and why is cancellation cooperative?",
"PYI_16_016": "How does the `asyncio.timeout()` context manager bound an operation's duration, and what exception does the caller see from outside?",
"PYI_16_017": "Why should cleanup after a `CancelledError` usually end with a re-raise rather than swallowing the cancellation?",
"PYI_16_018": "When does `asyncio.shield()` protect an inner Task but not cancel the caller's own cancellation?",
"PYI_16_019": "When is `asyncio.to_thread()` appropriate for a blocking function inside an asynchronous application?",
"PYI_16_020": "Why can't awaiting cancellation on `to_thread()` forcibly stop a synchronous function already running in the worker thread?",
"PYI_16_021": "Why can concurrent coroutines have a race condition on shared state even within a single OS thread?",
"PYI_16_022": "How do you choose between `asyncio.Lock`, `Event`, `Condition`, and `Semaphore` for a coordination protocol?",
"PYI_16_023": "Why is `ContextVar` better than thread-local storage for request-scoped state across asyncio Tasks?",
"PYI_16_024": "How do a bounded queue and a concurrency limit create backpressure and protect an async service from unbounded fan-out?",

"PYI_17_001": "When is `collections.ChainMap` better than eagerly merging dictionaries for layered configuration with precedence between the command line, environment, and defaults?",
"PYI_17_002": "How does `Counter` simplify frequency analysis, and how does it behave with missing keys?",
"PYI_17_003": "When can a `defaultdict` factory create an unwanted mutation from a simple key read?",
"PYI_17_004": "When is `itertools.chain()` preferable to concatenating materialized collections?",
"PYI_17_005": "How is `islice()` useful for a lazy iterable that doesn't support indexing?",
"PYI_17_006": "Why does `itertools.groupby()` only group consecutive equal keys, and when does the input need to be sorted first?",
"PYI_17_008": "How does `cached_property` differ from `property` in caching, attribute writes, and invalidation?",
"PYI_17_009": "What boilerplate does `functools.total_ordering` remove, and what performance and traceback trade-offs should you take into account?",
"PYI_17_010": "Why does `pathlib.Path` convey path intent better than a plain string?",
"PYI_17_011": "When is it fine to stay with `os` for a filesystem operation, and when should you choose `pathlib`?",
"PYI_17_012": "How does a naive `datetime` differ from an aware `datetime`, and why is mixing them dangerous?",
"PYI_17_013": "Why is `zoneinfo.ZoneInfo` better than a fixed UTC offset for civil time with daylight-saving transitions?",
"PYI_17_014": "When is `heapq` preferable to a full sort for top-k or a priority queue?",
"PYI_17_015": "When is `bisect` useful for a sorted list, and why doesn't binary search make insertion cheap?",
"PYI_17_016": "Why should library code log through a named logger instead of configuring the root logger on import?",
"PYI_17_017": "When should you choose `dataclass` for a value-like record, and when `Enum` for a closed set of symbolic values?",
"PYI_17_018": "When is runtime introspection via `inspect.signature()` justified, and when does an explicit protocol reduce coupling to implementation details?",
"PYI_17_019": "Why can a pattern with nested or overlapping quantifiers cause catastrophic backtracking on crafted input, and how do you reduce ReDoS risk?",

"PYI_18_001": "What boundary does a unit test have, and why shouldn't it quietly turn into an integration test?",
"PYI_18_002": "What contracts between components does an integration test check?",
"PYI_18_003": "How do you balance unit, integration, and end-to-end tests for feedback speed, fidelity, and maintenance cost?",
"PYI_18_004": "What seams are needed so a test doesn't depend on a real network, clock, or external service?",
"PYI_18_005": "Why does dependency injection often make Python code easier to test deterministically?",
"PYI_18_006": "How do you reproduce timeout and retry behaviour without a flaky dependency on a real external service?",
"PYI_18_007": "How does fixture lifecycle help set up state, yield a resource, and guarantee teardown?",
"PYI_18_008": "When is parametrization better than copying one test body for several cases?",
"PYI_18_009": "How can a broad-scoped mutable fixture create order-dependent tests and state leakage?",
"PYI_18_010": "For a repository that must behave like real in-memory storage, when should you choose a Fake, and when a Stub or an interaction-verifying Mock?",
"PYI_18_011": "Why must you patch the name at the place where the code under test looks it up during `patch()`?",
"PYI_18_012": "How do `spec`, `spec_set`, and autospeccing help a mock detect drift from the real interface?",
"PYI_18_013": "How does over-mocking implementation details make a test suite brittle during behaviour-preserving refactoring?",
"PYI_18_014": "When should you use `AsyncMock`, and what does such a test need to check besides the call arguments?",
"PYI_18_015": "How does replacing the real clock with an injected clock or frozen time make timeout and expiry tests deterministic?",
"PYI_18_016": "When does property-based testing with generated examples and shrinking find edge cases better than hand-picked examples alone?",
"PYI_18_017": "Why doesn't 100% line coverage prove that all important behaviours and assertions have been checked?",
"PYI_18_018": "How do you tell a flaky test apart from a genuine product race, and how do you preserve evidence to reproduce it?",
"PYI_18_019": "What does a surviving mutant after an artificial change to the code's behaviour mean, and how does that reveal weak assertions or missing cases?",

"PYI_19_001": "How does a deterministic profiler measure call counts and time, and why must its instrumentation overhead be taken into account when interpreting results?",
"PYI_19_002": "What problem does `timeit` solve compared to a single wall-clock time measurement?",
"PYI_19_003": "How can warm-up, caching, system load, and input distribution skew a microbenchmark result?",
"PYI_19_004": "How do you compare a `cProfile` run with a control run on the same workload, to tell instrumentation overhead apart from a real bottleneck?",
"PYI_19_005": "How do you verify that an asymptotically better algorithm actually wins on representative input sizes and distributions?",
"PYI_19_006": "How does choosing between `list`, `set`, `dict`, `deque`, and a heap depend on the workload's dominant operations?",
"PYI_19_007": "Why isn't Big O enough to predict latency without the input size, constants, and memory-access behaviour?",
"PYI_19_008": "Why can repeated string concatenation in a loop cause repeated copying, and when is it better to collect the accumulated fragments with `\"\".join(...)`?",
"PYI_19_009": "How can shallow and deep copying increase allocation cost on a hot path?",
"PYI_19_010": "How do `tracemalloc.reset_peak()` and phase-specific measurements help tell a temporary allocation spike apart from retained memory growth?",
"PYI_19_011": "Why must the arguments to a function decorated with `lru_cache` be hashable, and what happens if you pass a `list` or `dict`?",
"PYI_19_012": "How do you design cache invalidation and eviction when the source data can change?",
"PYI_19_013": "How can task batching and `chunksize` reduce serialization and scheduling overhead in a process pool, while worsening load balancing?",
"PYI_19_014": "Why can adding more workers hurt throughput because of contention, context switching, and downstream saturation?",
"PYI_19_015": "How can batching reduce per-call I/O overhead while increasing latency and memory use?",
"PYI_19_016": "When does moving a hot loop to a vectorized or native implementation justify the boundary-crossing and data-conversion cost?",
"PYI_19_017": "A new data-processing variant is only 2% faster on a representative benchmark but noticeably more complex: how do you justify the choice based on latency budget and maintenance cost?",
"PYI_19_018": "Why can premature micro-optimization increase defect risk without a noticeable user-level benefit?",
"PYI_19_019": "Why doesn't a single positive `tracemalloc` snapshot diff after a workload prove a leak, and how do repeated steady-state measurements tell cache growth apart from unbounded retention?",
"PYI_19_020": "Why doesn't growing RSS prove a Python object leak, and what additional measurements are needed?",
"PYI_19_021": "Why is it unsafe to apply `lru_cache` to a function whose result depends on the current time, randomness, external mutable state, or side effects?",

"PYI_20_001": "What does `events = []; enabled = False; enabled and events.append(\"run\"); print(events)` print, and why didn't the side effect happen?",
"PYI_20_002": "What does `a = [[1]]; b = a.copy(); b[0].append(2); print(a)` print, and how does a shallow copy create this aliasing bug?",
"PYI_20_003": "In Python 3.14, what does `match 1: case x: pass; print(x)` print, and how does the capture pattern affect the name's scope?",
"PYI_20_004": "The class `Key` defines `__eq__` based on `value` but does not define `__hash__`: what happens with `{Key(1): \"x\"}`, and how do you fix the contract for an immutable key?",
"PYI_20_005": "Implement FizzBuzz for integers 1..N with ordered rules `[(3, \"Fizz\"), (5, \"Buzz\")]`, so that a new rule never requires a new branch.",
"PYI_20_006": "Implement a palindrome check: apply Unicode NFKC, `casefold()`, discard non-alphanumeric characters, and never build a reversed copy of the normalized string.",
"PYI_20_007": "Implement the sum of integers in an arbitrarily nested list: reject `bool`, raise `TypeError` for other types, and `ValueError` for a cycle in the lists.",
"PYI_20_008": "Design a copy operation for a tree with shared subtrees and possible cycles, preserving the graph topology without aliasing with the original.",
"PYI_20_009": "Implement a retry decorator with a tuple of transient exception types and `max_attempts`: it must preserve metadata and re-raise the last exception with its traceback.",
"PYI_20_010": "In a descriptor with `__set__(self, obj, value): self._value = value`, two instances see the last value written: why does this happen, and where should per-instance state be stored?",
"PYI_20_011": "Implement top-3 words for ASCII text: a token matches `[A-Za-z]+`, comparison is case-insensitive, and ties are sorted lexicographically.",
"PYI_20_012": "Implement an anagram check for Unicode strings: apply NFKC and `casefold()`, ignore whitespace, and take other code points into account.",
"PYI_20_013": "Implement a streaming RLE encoder that lazily yields `(character, positive_count)` tuples from an iterable of characters with O(1) auxiliary memory; empty input yields nothing.",
"PYI_20_014": "Move numeric zeros to the end of a list in place, preserving the order of the other elements and using O(1) extra space; do not treat `False` as a zero.",
"PYI_20_015": "Implement `fibonacci(n, first=0, second=1)` that returns a new lazy iterator on every call and yields exactly n values with O(1) state.",
"PYI_20_016": "Implement a decorator for measuring elapsed time that preserves the wrapped callable's metadata, return value, and exception behaviour.",
"PYI_20_017": "Design a thread-safe rate-limit decorator with an injected monotonic clock: up to X calls per rolling T seconds, raising an explicit exception on excess.",
"PYI_20_018": "Implement a context manager for a transaction-like resource that commits on success, rolls back on exception, and never hides a failure.",
"PYI_20_019": "Implement a validation descriptor that stores values separately for each instance and works with two managed attributes.",
"PYI_20_020": "Design an immutable value object with correctly consistent `__eq__` and `__hash__`, suitable as a `dict` key.",
"PYI_20_021": "For `is_prime(n)`, which checks divisors up to `int(sqrt(n))`, write tests that kill the mutants `<` vs `<=` at the square boundary and the bug for `n < 2`.",
"PYI_20_022": "Write tests for a Two Sum API that returns the indices of the first pair in lexicographic order, never uses an element twice, and returns `None` when there is no solution.",
"PYI_20_023": "Write tests for a maximum-overlapping-intervals function: intervals are closed `[start, end]`, so start and end at the same instant count as overlapping.",
"PYI_20_024": "Write boundary and round-trip tests for an RLE decoder with the grammar `count:character|count:character`: count is a positive decimal, and a character can never be `:` or `|`; malformed input raises `ValueError`.",

"PYI_21_001": "What is the time complexity of the code `for i in range(n): for j in range(i): work()`, and what sum explains it?",
"PYI_21_002": "Why does Big O drop constants and lower-order terms, yet they still matter for real input sizes?",
"PYI_21_003": "For a stream of jobs, when should you choose a hash table for lookup by ID, and when a min-heap for repeatedly extracting the lowest priority?",
"PYI_21_004": "When is `deque` better than `list` for a queue or a sliding-window algorithm?",
"PYI_21_005": "What preconditions does binary search need, and why can inserting into a sorted list still be O(n)?",
"PYI_21_006": "When should you choose BFS for graph traversal, and when DFS?",
"PYI_21_007": "How does memoization or bottom-up dynamic programming change the complexity of a recursive solution with overlapping subproblems?",

"PYI_22_001": "Why can an index speed up a read query but increase storage and write cost?",
"PYI_22_002": "How do you write a query that returns all customers, including those with zero paid orders, and where should the predicate go so it doesn't effectively turn the `LEFT JOIN` into an `INNER JOIN`?",
"PYI_22_003": "After a failure between the debit and the credit, a transaction must not leave a partial transfer: which ACID property does this illustrate, and what does the application still have to decide about retrying?",
"PYI_22_004": "How does the isolation level affect concurrent anomalies, and why can stronger isolation require retrying a transaction?",
"PYI_22_005": "How do primary key, foreign key, unique, and check constraints move data integrity from application code into the schema?",
"PYI_22_006": "Why does a parameterized SQL query protect against injection, while string formatting or manual escaping is not a reliable substitute?",
"PYI_22_007": "When is keyset pagination with a unique tie-breaker in `ORDER BY` better than `OFFSET/LIMIT` for performance and stability under concurrent writes?",

"PYI_23_001": "How does a merge commit differ from a rebase in history shape and commit identities?",
"PYI_23_002": "When is rebasing a local, unpublished branch appropriate, and when does it dangerously rewrite shared history?",
"PYI_23_003": "When should you choose `git revert` for undoing something in shared history, when is `git reset` acceptable for unpublished commits, and how does reflog help recover a lost local tip?",
"PYI_23_004": "A change touches payment calculation: what is the minimum required CI gate of unit, integration, and static checks you should choose given the failure risks?",
"PYI_23_005": "When should you choose rolling, blue-green, or canary deployment, based on blast radius, traffic control, and rollback speed?",
"PYI_23_006": "How does running CI on every pull request shorten the feedback loop and reduce integration risk compared to infrequent manual integration?",
"PYI_23_007": "When should a reviewer approve an imperfect change that improves overall code health, and when do defect risk or new technical debt require more work first?"
}
